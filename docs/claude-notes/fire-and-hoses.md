# Fire and the hose weapons (flamethrower / acid spewer)

<!-- Split out of the old noita-sim note 2026-07-16. CLAUDE.md is the single source of
     truth and links here. Notes are dated and historical — trust the code over any line
     here, and correct the note when you find it stale. -->

## 2026-07-13 Noita-feel pass (branch `noita-sim`)

- **Fire (mat 10)**: probe-driven — water quenches to steam, ignites adjacent oil/gas 1/3/tick, chars Grass/Support/Ladder 1/50, decays 1/42 fuelled vs 1/8 starved, flicks upward, spits embers. Gas ignition (lava) now yields Fire, not smoke → flame fronts. Fire wakes neighbours on TryMoveTo arrival (sleeping oil pools must catch).

## 2026-07-14 combat/movement pass

- **Flamethrower/acid spewer**: 2x slower cadence (cd 0.11/0.12), 6 cells/puff, wider fans, double per-hit damage to hold dps; Particles jets 10/9 droplets + smoke/vapour curls.

## 2026-07-14 big 15-item combat/world/ecosystem batch (branch noita-sim)

- **Fire on flammable surfaces**: Cells.IsFlammable now includes Fernleaf/Platform/TreeTrunk/Canopy/Canopy2/TreeRoot/SeaFrond (biome-adapted flora stays immune). Lava ignites them via new TryIgniteTile in TickLava. Global **fire-spread throttle** `_fireBudget` (max 90, regen 22/s, SpendFire()) gates NEW-tile ignitions + ember spits + lava ignition so fire can't cross a whole planet (oil/gas burning ungated). SimTest "fire gutters out" still passes.

## 2026-07-14 zoom / eruption / lizard / flame / gem-embed follow-up

- **Flamethrower/acid spewer**: TIGHT stream — spread 0.42→0.18 (flame) / 0.36→0.15 (acid), 6→3 cells/puff, cadence 0.11/0.12→0.055/0.06 so it reads as one steady stream not pulsing waves; per-puff dmg halved (52→26, 64→32; titan 80→40, 90→45) to hold dps.

## 2026-07-14 doors / hose-rework / clouds / deep-caves batch

- **Flamethrower + acid spewer reworked**: now a HELD stream that grows length the longer it's held — new Game1.StreamReach() (_streamHold ramps to StreamHoldMax=0.35s (was 1.1 — user wanted max length much quicker), reach lerps 42→132px; a >0.15s gap resets it, shared by both hoses). Particle jets take a `reach` param and set **CollideTiles=true** so flame/acid/smoke splash on walls instead of ghosting through them (EmitFlameJet/EmitAcidJet + hero-flicker + cinders all reach-scaled + collidable). Cells launched at reach*2.1 speed, 3/puff, spread 0.18/0.16. Cooldown 0.05, but the "machine-gun" per-shot sound fixed: flamethrower/acid_spewer play their ShotSound on minGap 0.32 (a sustained low roar) instead of 0.03 — see the shot-sound block in Game1. Noita-look pass (2026-07-14): particles ARC WITH THE CELLS — `HoseArcGravity = 2.25` (cells pull FlyGravity≈450 px/s², particles 200×scale → 2.25 matches) replaces the old rise/no-arc scales, so the visible tongue droops exactly onto where the payload lands; 9 blobs + 3 smoke/vapour curls per puff (was 5+2), spread 0.24/0.22, bigger sizes (hot 1.8 / body 2.4-3.2), smoke curls seeded ALONG the tongue (pos + d*reach*0.35) that buoy up after.

## 2026-07-15 fire-vs-snow / water-quench / gem-bed pass

- **Fire melts snow**: TickFire probe — a flame beside a Snow tile (1/6 per probe, NOT fire-budget-gated) melts it to Sky + Water(Density/2) + steam; the meltwater then douses the flame naturally. Lava already melted snow via IsMeltable.
- **Fire lingers + catches better**: gutter roll fuelled 42→56, starved 8→16 (~0.9s/0.27s); flammable-tile char rate 1/50→1/28 (still SpendFire-gated).
- **Flying fire quenches on water**: UpdateFlying contact branch — a flying Fire cell whose blocking cell is Water flashes to a Smoke wisp at its last free spot (never banks burning cells on/under pools). FireFlamethrower with the MUZZLE in water just places a Smoke bubble and returns (can't light underwater).
- **Acid vapour rain fixed**: EmitAcidJet wisps were seeded mid-air along the rope (pos + d*reach*0.35) and fell like rain — now 2 wisps seeded AT THE MUZZLE riding the rope's arc (HoseArcGravity*0.8). Flame smoke curls stay mid-tongue (reads as smoke, user didn't flag).

## 2026-07-14 acid-depletion / granular-hose pass

- **Granular hose pixels**: EmitFlameJet/EmitAcidJet blobs shrunk to ~0.8-1.5px (was 1.8-3.2) and count bumped 9→14 (+smoke/vapour 3→4, also ~1.1-1.6px) — many tiny grains = Noita pixel-fluid look, same arc/collide/reach behaviour.

## 2026-07-15 fine-pixel-fidelity 10-pack (user: "do all those including bigger swings")

- **ShedBurningLeaves** (both TryIgniteTile + TickFire char): leafy kinds (canopy×2/Fernleaf/SeaFrond/Grass) launch 2-3 falling flying-Fire embers — canopy fires rain glowing motes that can spot-fire or die in steam.

## 2026-07-15 effects-as-materials — IN WORKTREE `cell-effects`, NOT merged (user wanted it isolated for testing; worktree at `.claude/worktrees/cell-effects`, branched off noita-sim; auto-committer commits there too, so no single feature commit)

- **Cinder→fire handoff**: `Particle.LandMat` byte (Material stamped where a CollideTiles particle comes to REST, cleared after one stamp; chance rolled AT EMIT — EmitCinders gives 1-in-4 coals Fire). `Particles.Update(dt, planet, cells?)` — Game1 passes `_run.Cells` at all 7 sites; PerfTest/headless callers omit it. So every explosion/flamethrower/volcano-vent cinder can spot-fire where it lands (sim's own fire budget throttles spread).
- Round 11 (user: "spraying stretched pixels super fast, want slow long streams"): hose jetSpeed reach*2.6→1.9 with grain life 0.4→0.6 max (same tongue length, half the flow speed) + Game1 payload launch reach*2.1→1.7 BOTH hoses (visible stream and cell payload must share an arc); smear ~2 frames (0.022→0.035) cap 6→12×size. Screenshot: stream = bundle of long parallel flame strands ✓.
- Round 12: **constant hose flow** — jetSpeed pinned `const 80f` (the tap-fire start speed; the hold-ramp had scaled it 80→250 with reach), hold now lengthens tongue via `lifeScale = reach/42` on grain Life; Game1 payload launch pinned `72f + rand` BOTH hoses (arc-match) → effective hose range is now the tap-fire range at every hold length (deliberate, per user). **MERGED cell-effects→noita-sim 2026-07-15** (~08:30): merge-race pattern worked (merged noita-sim INTO worktree first, resolved 3 conflicts — Program.cs probe arms both-kept, RunSave v24 over their v23 HasGrapple note kept, Game1 stepped-zoom + their titan fight-zoom COMBINED: fractional fight-zoom auto-falls-back to direct render path — suite PASS on merged tree, then merged back clean; auto-committer completed the conflict-resolution commit itself). Worktree still exists post-merge; both branches at same tip.
- Round 14: constant-flow REVERTED (user: wanted slow flow NOT short range) → jetSpeed reach*1.4 + payload reach*1.7 restored, lives 0.5-0.85s (travel≈reach at any hold), smear 3 frames/16×; the /btw fork verified-no-op (parent had already applied).
- Round 15 tune: StreamHoldMax 0.35→0.245s, reach 42-132→59-185px, ALL four spread cones ×0.4 (payload 0.072/0.064, glow 0.068/0.064 rad).
- Round 16 (user: "waves of 5 + stray yellow strands out-ranging the stream"): **de-pulse** = per-grain emission-time head start (`lead = rng*0.06s`, Position += vel*lead, Life -= lead) in BOTH hoses so puffs interleave into continuous flow; the overshooters were (a) hose-shed CINDERS at reach*0.9 with ember gravity 0.55 flying flatter+further than the 2.25-gravity tongue → reach*0.4, and (b) buoyant lick grains → seeded ≤reach*0.4, life ≤0.35, drag 3.2 (die within the tongue).
- Round 17: glow jetSpeed 1.4→**1.7 = EXACTLY the payload multiplier** (any gap = landing fire beyond/short of the visible tip — "the mismatch"; keep these two locked together) + **every hose grain stamps its material on landing** (flame grains + licks LandMat=Fire, acid droplets LandMat=Acid — the whole visible spray burns/corrodes, not just the 3 payload cells; acid self-depletion + fire budget bound it, but WATCH acid strength in playtest: ~8× more corrosive mass per puff). Smokeprobe airburst check made delta-based (ground-burst plume drifts into the count zone after 8 sim-s). All merged to noita-sim.
- Round 19 (user: yellow embers back + want land-sparks): lick grains AND hose cinder shed REMOVED outright (both rode non-stream arcs — rising -0.5 / ember 0.55 gravity vs the tongue's 2.25 — and kept resurfacing as stray yellow strands; their burn-where-it-lands job is covered by the per-grain Fire stamp); new `Particle.LandSparks` one-shot flag → resting grain throws 3 tiny sparks in its OWN stepped-fade colour (kicked up along local normal + tangent; spawned sparks carry no flags so no cascade) — set on all flamethrower main grains.
- Round 21 (yellow streaks ROOT CAUSE, screenshot-proven): aiming skyward, glow out-raced payload because flying cells obey **FlyMaxOutward=170** and particles didn't → hose emitters now take `up` and mirror the exact cap on grain velocity (KEEP glow+payload caps identical); airborne fire cells were the pale-yellow flicker (garbage tier probe at world coords) → ColorFor `airborne` param pins flying fire to mid tier; flow slowed reach*1.7→1.35 BOTH (user: still too fast), lives→0.95/0.92 max.
- Round 22: **flamethrower LaunchAtWorld payload REMOVED entirely** (user: still off-arc — flying cells can never share the particle arc exactly: FlyMaxOutward, separate jitter) — the grains ARE the flame now (lives 0.8-1.35s, every grain stamps Fire on landing, cone ignition burns creatures); acid spewer KEEPS its payload (corrosion mechanic). Compaction voided-pile test observed flaky (fails 1-in-2 runs, passes clean rerun — rng-sensitive settle, predates this change).
- Round 23: **EmitJetCore** — flamethrower + acid spewer are ONE shared stream core (tones/hotTones/fade/landMat/lights/drag are the only per-weapon params); acid payload ALSO removed (droplet Acid stamps = the corrosion mechanic now); cone 0.068/0.064→0.041 (-40%); hoses carry PRIVATE SmearMax 8.8 (= frozen old shared look) — **contract: hose tuning goes in EmitJetCore only and touches nothing else; shared Draw smear/fade changes no longer affect hoses or rain**.
- Round 24: falloff/landing spread -30% = speed band 0.85-1.15→0.895-1.105 (range∝v², so the SPEED band is the landing scatter, not the cone).
- Round 25 (stream detached from gun at full ramp): de-pulse position advance scaled with vel (≈15px at full hold) → capped 6px (temporal stagger keeps full range), seed jitter 5→1.5px, muzzle offset 8→4px both hoses.
- Round 26 (still waves, esp. moving+firing): **hoses fire EVERY FRAME** (ShootCooldown 0.05→0f) with 8 grains/frame (~same rate) — emission continuous by construction, de-pulse lead machinery deleted, grains born AT the muzzle; COMPENSATED: damage constants ÷3 (42/62/50/70→14/21/17/23, they're ×_frameDt per CALL so call-rate ×3 = dps ×3) and StreamHoldMax 0.245→0.735 (StreamReach adds _frameDt per call — the old ramp was secretly 3× the constant in real seconds; 0.735 real = same feel); hero flicker gated 1-in-3 frames (ray-cast budget), soot 6→2/frame, vapour 3→1/frame; strands 2× via new Particle.SmearScale (hose SmearMax 17.6). TRAP: any future per-call hose constant must account for per-frame calling.
- Round 27 ("shooting spaghetti while flying"): hose grains **inherit shooter momentum** (`vel += shooterVel` AFTER the outward cap on the launch component; soot/vapour wisps too; Game1 passes Player.Velocity) — world-relative launch painted the muzzle's flight path in fire; standing-still look unchanged by definition. Slow flow makes player speed a big fraction of stream speed, which is why this only surfaced after the slowdown rounds.
- Round 28: muzzle = DRAWN gun tip (grip 3.2 + texWidth-1.5 × 0.39 held scale, per-weapon via _weaponTex; the orb's aim*9f was the hint); births back-filled along shooterVel×rng×0.016 (moving-muzzle stair rows); grains -20% (skip 1-in-5); LandMat stamp chance 100%→50% both hoses.
- Round 29 ("fluid like water"): **hose streams render as metaball bodies** — grains carry `Particle.Fluid` (Material tag) + `Particles.FluidMode`; DrawFluid draws CAPSULES (len ≥5px / ~2 frames of travel along vel, width 4 — round blobs made a meatball chain, capsules bridge the inter-frame batch gap) into coverage RTs under LiquidFillBlend; ACID grains join _liquidRt (stream fuses with pools); FLAME gets own Game1._flameRt (fire must never metaball-fuse with water/acid), composited via the same CompositeLiquids shader right after pools; strand-quad fallback when shader off/zoomed out (FluidMode false). **Outward 170 cap DELETED from EmitJetCore** (bent aim + split arc families when firing up; payload gone, grains ≤1.35s). DM_LIQRT=0 kills pools+stream fluid together.
- Round 30: **EmitJetCore DELETED — hoses split into fully independent EmitFlameJet/EmitAcidJet per user** (tune individually going forward; the round-23 coupling contract is REVOKED); flame soot flecks + acid vapour wisps removed (fast enough to smear = read as leftover stream strands around the fluid body); remaining flame extras: hero flicker (invisible light carrier). Each emitter owns its full constant set inline.
- Round 31: **fire-cell BURN FUSE** — fire never uses _srcTile, so it carries a fuse there (TickFire: fuse>0 → no gutter roll, decrement 1-in-3/tick; ~3×fuse ticks guaranteed burn; rides the save free); StampAtWorld gains fireFuse param, Particle.LandFuse carries it; flamethrower grains stamp fuse 50-80 (~2.5-4s standing burn on ANY surface — spread still flammability+budget gated, ember-spit still fuelled-only). ALL fire-creation paths must zero _srcTile (IgniteCell did, gas-ignition now does). Smokeprobe cinder sampling 30→8 ticks (was missing 0.8s fire peaks).
- Round 32 (flame pooled like liquid on landing): (1) flame grains (Fluid==Fire) DIE 0.1s on rest — the resting-grain metaball puddle was the pooling; the fuse-burning CELL fire is the standing flame; (2) CompositeLiquids gained a param overload — flame RT composites opacity 1.0 + rim 1.85/+0.15 (hot sheath) vs pools' 0.80/1.45/0.08 wet lip; (3) flame land-sparks are BUOYANT licks (gravity -0.45, tangent 20, life ≤0.38) vs liquids' falling spatter. All flamethrower-only; acid untouched.
- Round 33: fuse 120-180 (6-9s ground burn), char rate 28→90 (~3× slower — burning structures burn DOWN not vaporise; ignition reliability = fuse dwell time).
- Round 34 fire-jet look (items 1-3 of 4): DrawFluid fire branch — blob width lerp 2.5→7px by age (taper/bloom), colour × lerp(1→0.5, age²) (faint tip blobs = coverage tatters at the threshold = ragged boiling end); Update — Fluid==Fire grains get bounded lateral turbulence (perp × sin(Life×55) × 90 × age × dt; Life-clock phases scatter free). Item 4 LANDED (round 35): 12Hz body flicker in DrawFluid fire branch — `c *= 0.85 + 0.25·sin(Time·75 + (Life+Time)·40)`; TRAP: the per-grain phase key MUST be `Life + Time` (constant per grain = birth time + initial life) — keying off Life alone strobes 60Hz white-noise as life ticks.
- Round 36 (user: 3D-game flamethrower, stream disappears into plume; ref = Blender FT tutorial vid): **EmitFlameJet rebuilt as CARRIER + PLUME** — 3 invisible carriers/frame (Size 0; unchanged arc physics; carry LandMat+fuse+LandSparks; own fire tones ONLY so the spark splash has ink — Transparent carriers made invisible sparks) + 6 plume puffs/frame (THE visible fire: launch with stream, drag 1.5 stalls them late-life, gravity -0.3 buoyant lift off the arc, FadeColor smoke-grey 75/60/55 so flame gutters into smoke, Fluid=Fire → metaball + turbulence + flicker; blob width now 3→11px by age = billow); wider plume cone (coneArc×2.2). Acid keeps the round-30 solid-rope form.
- Round 37: plume GravityScale -0.3→HoseArcGravity (young puffs DROOP WITH the stream) + **age-growing buoyancy in Update** (`vel -= GravityAt × 200 × 4.5 × age × dt`, Fluid==Fire only) → net 2.25g→-2.25g over life: fire tracks the arc then tears upward, divergence peaks at stream end; FlameTones deepened (255,235,150 / 255,180,50 / 255,120,25 / 230,75,20 — old set read gas-torch pale); +spark streaks (1-in-2/frame, 1.1-1.4× stream speed, SAME arc gravity, smeared, quick); +smoke billows — **ALL OF ROUND 37 REVERTED per user immediately after** (flamethrower is back at the round-36 carrier+plume state: pale FlameTones, plume gravity -0.3 buoyant, no divergence lift / sparks / smoke billows; restored via `git checkout <palette-commit>^ -- Particles.cs`). Don't re-apply round-37 ideas without the user asking.
- Round 38 (user then asked for two of them differently): plume FadeColor smoke-grey 75/60/55 → deep flame red 205/75/15 (**no black/grey anywhere in the jet**), DrawFluid tip dim floor 0.5→0.72 (heavy dim on red = brown-black; tips tatter via coverage instead); +spark pixels —
- Round 39: sparks now SHED BY THE PLUME GRAINS THEMSELVES (Update Fluid==Fire clause, 1-in-60 per grain per tick ≈ 5-6/frame at full stream; Position = grain's, Velocity = grain×0.6 + Jitter(45)) so they're born on the actual burning body — the earlier aim-ray seeding put them off the drooped/moving flame; smoke billows likewise shed sparks (Fluid=Fire) — acceptable, embers off smoke.
- Round 40: sparks get `Particle.JetSpark` — same turbulence wander as the jet body (shared clause `Fluid==Fire || JetSpark`; shed still gated `!JetSpark` = no recursion), SmearMax 1.5px (was ~8px default streaks), GravityScale = symmetric ±1.6 random (half curve up as hard as half sag — dancing flecks, not falling grit).
- Round 41: shed 1-in-60→1-in-20 (3× sparks, ~17/frame full stream); JetSpark added to the die-on-rest clause (0.1s) — lit sparks were parking 1.4s as glowing embers under the cinder rule and READ as igniting surfaces (they never stamped fire).
- Round 42: sparks CollideTiles=false — even the bounce/ricochet read as ground interaction; sparks are pure light now, only the carriers touch the world.
- Round 43 (STILL ground sparks — the real sources): (a) carrier LandSparks touchdown splash REMOVED (3-fleck burst at every landing = sparks born at ground contact); (b) spark shed speed-gated sp>3600 (grounded/stalled plume grains were shedding onto the terrain). Flamethrower ground contact = fire stamp ONLY now. Acid keeps its LandSparks liquid spatter (separate weapon, not complained about).
- Round 44 (jet wasn't igniting): carriers bounce at ~250px/s and often EXPIRED mid-skitter without ever resting → LandMat never stamped; FIX = fuse-carrying grains (LandFuse>0 = hose carriers only) stamp on FIRST CONTACT + velocity zeroed so the rest branch fires the contact spark burst same frame (LandSparks restored on carriers — it's the STREAM's touch effect, distinct from flying sparks which stay world-inert); char 90→45 (middle: touched surfaces catch within a beat, structures still burn down slowly). Rest-based handoff unchanged for cinders/debris.
- Round 45 (sparks at feet firing straight UP): carriers are BALLISTIC and outlived the visible plume — up-shots arced back onto the shooter (~1.1s return) stamping fire+bursts where no flame showed; carrier Life 0.8-1.35→0.65-0.85s = full horizontal range (~0.75s) but dies at the apex, return arc impossible.
- Round 46: **CARRIERS DELETED** (user: 'why have invisible carriers at all' — every phantom-fire bug traced to their ballistic arc mismatching the buoyant visible flame): plume puffs ARE the weapon — 50% carry LandMat+fuse+LandSparks and stamp on FIRST CONTACT (the fuse>0 contact trigger); buoyant puffs can't arc back → up-shots structurally can't rain fire on the shooter. Ignition range = where visible flame reaches (~jetSpeed/drag ≈ stream length) — expectation-aligned by definition. The carrier concept is DEAD, don't reintroduce it.
- Round 47 (licking burn-site flames): **fuse top-up** — StampAtWorld re-stamp onto an already-burning fire cell adds fireFuse/2 capped 80 (=4s fuel-less; initial stamp fuse 30-42 ≈ 1.5-2s) → burn duration IS jet dwell time; **Cells.PendingFlames** (PendingBubbles pattern, cap 200) — burning cells (fused OR fuelled) queue 1-in-12/tick → Game1 drains → Particles.EmitLickingFlame (rising tongue, Fluid=Fire joins metaball body, no world interaction, below spark-shed speed gate) — standing fires read as actual writhing FLAME and the visuals spread wherever fuel spreads the real fire.
- Round 48: licks are NOITA PIXEL FIRE not fluid (Fluid tag removed — ground flame = crisp 0.5-0.8px grains flickering up the stepped ramp; only the JET is a metaball body; licks carry JetSpark for the turbulence wander + SmearMax 1.2, no collision); jet spark shed 1-in-20→1-in-7 (3× again, ~50/frame full stream).
- Round 49: shed 1-in-7→2-in-7 (2×, ~100/frame full stream); spark velocity 0.6→0.78 stream inheritance + Jitter 45→31 (30% more in-line, less radial flying).
- Round 50: shed →4-in-7 (2× again, ~200/frame full — spark counts getting perf-relevant, watch pool size), Jitter 31→26 (envelope -15%).
- Round 51: plume TAIL-END = black smoke (DrawFluid fire branch: age>0.7 hue-shifts to 48/43/44 in TWO hard steps, alpha/coverage preserved so the wisp keeps shape; body stays pure fire — this coexists with round-38's "no black" because it's the die-off only, per user).
- Round 52 flame-sites tried and REVERTED next round ("did almost nothing" — ~8 half-px flecks alive per site is invisible).
- Round 53, the working answer: **density of WORLD fire, not more particles** — `Cells.StampFireBlob` (contact stamps 5 fused fire cells in a 2.5px disc; occupied fire = fuse top-up) so the ground itself burns Noita-style (cells dance/tier-shade); PendingFlames queue rate 12→5 + lick life 0.4-0.75/speed 18-44 (licks ride the denser cell population). Lesson: burn visibility scales with BURNING CELLS; particles alone can't carry it at 0.55px cap.
- Round 54 (flame shape): EmitLickingFlame tapers like a real flame — base offset s0 ±2px, side velocity −s0×5 (drag bleeds it) so paths CONVERGE rising, outer licks' life ×(1−|s0|×0.12) (edges exist only low → triangle silhouette), 1-in-4 TALL tongue (up 34-64 vs 18-36, life 0.55 base) = the dancing tip.
- Round 55 (base too dim, fades too low): licks + jet sparks now set **MaxLife = initial Life** — with a fixed MaxLife most were BORN mid-ramp (t0 = Life/MaxLife < 1 = pre-cooled); now every one starts white-hot at the base and cools across its own full path. TRAP: any emitter whose Life is randomized below its MaxLife spawns pre-faded — check t0 when adding emitters.
- Round 56 (strongest part must STICK to the surface, die top→base→smolder): **fuse-driven flame pyramid** — PendingFlames entries now carry the cell's remaining fuse (fuelled floor 55); EmitLickingFlame(pos, up, fuse): strength=fuse/80 → 3 layers: 55% BASE CHURN (near-stationary white-hot grains boiling AT the surface, gravity 0, rise 3-10), 30% rising body (rise ×strength), 15% tall tongues ONLY while strength>0.5; strength<0.25 = SMOLDER (dim ember reds 200/70/20 & 150/45/18, hugging surface).
- Round 56 pyramid layers REVERTED (round 57, "did not work as intended"): back to the round-55 taper shape; new rule = **vigor = 0.45 + 0.55×(fuse/80)** multiplying BOTH lick lifetime and velocity — as the ORIGIN's fuel drains, everything risen above it expires faster and hugs lower, while the origin's fuse-backed burning cell outlasts it all (the user's model: origin holds the most fuel, dies last).
- Round 58: vigor scaling UNDONE per user; the settled design = **height-ambition continuum**: per spark h=rng — rise lerp(3,52,h²), lifetime INVERSE lerp(0.85,0.25,h)×(0.8-1.2 jitter), buoyancy -0.35×h, convergence (1.5+3.5h) — long-glowing base-huggers at the origin, brief tall tongues, full spread between; fuse param kept but UNUSED.
- Round 59 = the REAL fix (rounds 57-58 were symptom-chasing, both reverted): TickFire's upward flick moved FIRE CELLS off the surface with the fuse riding along — the origin went dark while the long burn finished mid-air (licks emitted at cell positions followed it up). Fix: **flick gated on _srcTile[i]==0** — fused (flamethrower) fire is ANCHORED to its ignition surface for the whole burn; unfused transient fire keeps dancing. Licks back to round-55 taper shape. Origin now structurally dies LAST.
- Round 60 (anchoring crippled fuel spread — the flick IS fire's transport): **three-state fused fire** — `anchored = fused && !fuelled && grounded` (grounded = any solid-tile neighbour, flagged free in the probe pass): (1) barren surface → anchored origin burn; (2) FUELLED → released to travel/spread, fuse still blocks gutter (stronger front than plain fire); (3) mid-air unfuelled → fuse drains -3/tick (~0.1s) + keeps dancing — no hovering anchors when fuel depletes in the air. Known edge: fire atop loose CELL piles (sand) counts ungrounded → fast drain (acceptable; extend grounded to matter cells if it reads wrong).
- Round 61: fuelled fire = INTENSE but SLOW-consuming — PendingFlames roll fuelled 1-in-2 vs bare 1-in-5 (2.5× licks over fuel), fuel-cell ignition 1-in-3→1-in-6 (pools last ~2× longer), tile char 45→60 (structures burn longer; catch reliability still carried by fuse dwell).
- Round 62 (fire couldn't spread DOWN): flame movement was up-only, so fire FLED fuel beside/below before slow ignite/char rolls landed; fix = fuelled fire's moves are 1-in-3 lateral-or-downward (dirRoll: down/left/right via InnerCell/±cx), rest keep the rising dance — fire now burns down trunks and descends oil films while unfuelled flame still only rises.
- Round 63 (trees STILL only burned up — crawl can't enter solid trunk; the real asymmetry is RESIDENCE TIME: rising fire piles against ceilings but evacuates floors): **cling** — flame whose floor tile is flammable (Probe below:true sets burningFloor) gets mobility ÷3, + **floor char 30 vs 60** — trees now burn down the trunk; up still faster (natural).
- Round 64 (CONTRACT: small tree fully aflame before first-lit part burns out): charring IS consumption so it can't be the spread mechanism — **BUDDING** added (fuelled flame births fire into a random free cardinal neighbour 1-in-25, SpendFire-gated) = direction-neutral engulfment decoupled from burn-through; fuelled gutter 56→130 (~2.2s front persistence); consumption slowed further: fuel-cell ignite 6→10, char 60/30→110/55; fuelled licks queue EVERY sample (was 1-in-2). Suite PASS ('fire gutters out' still green — budget caps the blaze).
- Round 65: **CONSUMPTION BUDGET split** — char rolls scale with flame population and budding GROWS the population, so no char constant could stop an engulfed tree eating itself (and chars fought buds over one budget); new `_charBudget` (cap 6, regen 2.5/s planet-wide) — BOTH char sites (TickFire probe + lava-side TryIgniteTile) spend it; rolls loosened back to 40/20 (budget is the limiter; lone burns stay responsive). Consumption is population-independent BY CONSTRUCTION: engulfment (~2s, SpendFire-budgeted buds) always beats burn-through; forest fires smoulder at the same global 2.5 tiles/s.
- Round 66 (too slow both ways): CharBudget 2.5→6/s (cap 12), budding 25→12 (engulfs ~1s), fuel-cell ignite 10→6. A ~15-tile tree: engulfed ~1s, consumed over ~3-4s — aflame-before-burnout contract still holds with margin.
- Round 67 (missed connected fuel + still slow): (A) **fuel-seeking buds** — Probe records a cardinal fuelMask; buds travel PERPENDICULAR to the fuel (fuel above/below → bud sideways, beside → vertically), wicking along surfaces instead of blind-random; rate 12→7. (B) **8-direction probes** — 4 diagonal Probe calls (InnerCell/OuterCell of cx±1, dirBit -1 = no mask steer): canopies connect at CORNERS which cardinal probes could never reach — the root cause of surviving fragments. (C) char budget 12/s cap 24 (2×). TickFire probes now 8-9/cell/tick — fire pop is bounded, fine.
- Round 68 — THE REWRITE (user: engulf-then-burn-out arc; emergent tuning had failed 6 rounds straight): **Cells.BurningTiles registry** (tile→clock; cap 400) + TickBurningTiles sweep: ignite → after 0.25s ignite all 8 neighbours (deterministic fuel-graph wavefront) → blaze 4-7s hash-stable (licks 1-in-5/tick + hazard fire cell 1-in-30 in adjacent sky) → char to Sky+fire+smoke+ShedBurningLeaves. RETIRED: budding, cling, fuel-seeking mask, diagonal probes, char budget (probes back to 4 cardinals; TickFire flammable branch + lava TryIgniteTile now just IgniteTile 1-in-20/24 SpendFire-gated; jet StampFireBlob ignites 3×3 tiles directly). **Rigid persistence**: BodyCell.Burn (clock+1; rides splits since it's IN the struct), SpawnBody steals via Cells.StealBurning, Stamp re-ignites at the LANDED tile (clock resumes), tumbling bodies QueueFlame — fire survives detach/fall/land. Registry NOT serialized (flying-cells policy). Suite+smokeprobe PASS. Tuning knobs: BurnSpreadDelay 0.25 / BurnDurMin 4 +3 var / MaxBurningTiles 600.
- Round 69 (stalls/uneven/eternal impotent flames): spread was a ONE-SHOT clock crossing — any ignition blocked at that tick (chiefly the 400-cap during a healthy blaze) was never retried → unlit adjacent fuel + fuelled cells lingering impotent; also resumed rigid clocks skipped the window entirely. FIX = spread retried all blaze long (`nc >= delay && rng18==0`, idempotent dict hits) + cap 600 — the cap is SOFT now, blocked ignitions heal as burn-outs free slots. RULE: never one-shot an event whose success depends on transient global state.
- Round 70 (big fires capped out, front stalled): **pressure aging** — clocks run ×(1 + (count − cap/2)/(cap/4)), max ×3, when the registry fills; the TAIL burns out faster to free slots so the FRONT keeps full pace (travelling fire band); cap 600→900. Front spread also quickens slightly under pressure (delay crossed sooner) — reads as a big fire raging.
- Round 71: **burn-out is structural** — Cells.PendingCharred (tx,ty,kind) queued on every char-out; Game1 drains → Physics.MarkDirty (settle detaches undercut regions as rigid bodies — fire severs like a pickaxe) + TreeTrunk → ToppleTree (rigid log fell); the felled log's burning tiles ride BodyCell.Burn and keep burning where they land. Burn-through a tree's base and the whole thing topples aflame.
- Round 72: falling-body flame was thinning at detach (double-gated licks ~5/s vs standing tile 12/s, and no hazard fire-cell layer on moving bodies) — fixed with tile-parity licks (Next(5) per burning cell per tick) + 1-in-30 ember shed via LaunchAtWorld(Fire) inheriting 0.8× body velocity, so tumbling logs trail embers and can spot-ignite mid-arc.

