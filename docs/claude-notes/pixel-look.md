# Pixel look: grain size, cell density, pixel-grid RT

<!-- Split out of the old noita-sim note 2026-07-16. CLAUDE.md is the single source of
     truth and links here. Notes are dated and historical — trust the code over any line
     here, and correct the note when you find it stale. -->

## 2026-07-14 zoom / eruption / lizard / flame / gem-embed follow-up

- **Zoom keys**: hold "-" (OemMinus/Subtract) to pull out, "+"/"=" (OemPlus/Add) to push in — adjusts `_playZoom` clamped [2.2, 8], set on _camera.Zoom each play frame (in the play update, just before _camera.Follow).

## 2026-07-15 fine-pixel-fidelity 10-pack (user: "do all those including bigger swings")

- **Damp ground tint**: `Renderer.WorldCells` (set by Game1 beside TreeBiome each frame; null elsewhere skips). Tile loop probes cell row `(r+1)*Density` (2 Gets) for water on the tile's outer face → ×(0.72,0.78,0.92) folded into BOTH `col` (flat/authored/crust) and `tintF` (atlas path — col alone does nothing for atlas tiles!).

## 2026-07-15 effects-as-materials — IN WORKTREE `cell-effects`, NOT merged (user wanted it isolated for testing; worktree at `.claude/worktrees/cell-effects`, branched off noita-sim; auto-committer commits there too, so no single feature commit)

- **Particle smoke demoted**: EmitExplosion smoke loop → ≤4 brief small puffs when CellFx; nuke's 14-particle rising column gated out entirely (its big crater budget covers it).
- **Noita-grain pass (same worktree, user: "small and plentiful, too chunky")**: EVERY Particles.cs emitter retuned — sizes ~0.7-1.4px (were 1-7), counts ×1.5-3 (explosion fireball strength*0.6→*1.8, hammer 14/10→26/22, dust 14→30, rocket exhaust 10→26, hoses 14→22, muzzle sparks 4→8…); explosion flash square strength*0.55 (14px on a nuke!) → min(3.5) heart + one-frame 1px white starburst, muzzle flash quad 3→1.6 (hero LIGHT is the flash); **lights moved to a SUBSET of grains** (every 2nd/3rd/4th) so tripled counts don't flood the lighting pass; Weather rain/snow 5→9 drops/tick at ~1px. Suite + smokeprobe green.
- Round 2 (user: "still too large"): **hard 1-world-px cap in Particles.Draw** — the ONE choke point, `min(Size, 1f)`, same grain as a sim cell; emitter Size>1 now only means "full pixel" (flash/exhaust hearts flatten to 1px; their job is the hero light). Any future "particles too big" report: the cap is in Draw, not the emitters.
- Round 3 (user: "still large — acid/fire/water/sand"): culprit was the CELL draw, not particles — Cells.Draw padded every quad +0.5px (seam bleed for rotated polar quads), so every isolated cell was a 1.5×1.5 square; fix = **neighbour-aware padding** (stride-1 only: bleed 0.5 per axis only toward IsBlocked neighbours — pools stay seamless, ground-bleed invisible; open-air sides 0.1 → lone droplet/ember/grain = crisp ~1px) + **area-conserving flying streaks** (width = 1/len, floor 0.55, so smears stay one grain of ink). Verified: lake body seamless, splash droplets pixel-crisp.
- Round 4 (user: "still too large/chunky"): ROOT CAUSE finally named — the terrain ATLAS (16×16 over 4px tiles) gives the world an apparent grain of ~0.25-0.5px, so even a true 1px cell quad reads 2-4× chunkier than the ground; fix = **EXPOSED cells (any open-air side) draw as 2×2 half-px sub-grains** w/ hashed shade 0.82-1.0 (pool/pile interiors stay single padded quads — quad budget bounded, sim untouched), flying streaks base 0.7px w/ area conservation, particle cap 1f→0.85f. The dynamic-matter grain ladder is now: cell skin 0.5px sub-grains / flyers 0.7px / particles ≤0.85px, all under the tile's 4px. Rounds 5-6: skin-ink/scatter/glint continuity change tried and REVERTED per user ("weird") except fire; ROOT-CAUSE trio then landed (user: "think hard"): (a) default `_playZoom` was 4.0 — effects drawn 4×4 screen px; (b) world res per character is half Noita's; (c) no pixel-grid discipline. FIX = **Density 4→8** (0.5px cells, dwarf ~18 grains tall; RunSave v24 — v23 reserved by titan pass; CompactMinFill was flat "-2" and silently tightened → now Density²/8 fraction; oil test 48-cell counts + perf bound 6→16ms Debug were Density-hardcoded) + **pixel-grid world RT** (Game1._worldRt: world layer renders at cam zoom 2 = 1 texel per 0.5px cell into Virtual/k target, ONE point-sampled integer upscale after EndEntities, lighting composites at native after; zoom keys now STEP {2,4,6,8} edge-triggered; landing/orbit non-step zooms fall back to direct path; DM_PIXELRT=0 disables) + sub-grain draw split REMOVED (cells fine enough now), pads made relative, flyer streaks in cell units, particle cap 0.55px. **Fire kept the dance** (per-grain 12Hz hash jitter ±0.22 in Cells.Draw) + **starved fire pools ~0.8s** (gutter 16→48). Perf at D8: Release meteor mean 2.65ms (was 0.4), worst 48ms spike (wake-storm class), Debug steady 13ms — watch for hitches in playtest. Suite PASS.
- Round 7 (user: "long load, 0 fps overhead"): load stall = seeding woke EVERY cell (sea+lakes ×4 at D8) then the porous sea (SpawnInTile random-with-replacement = accidental 63% fill) collapsed sea-wide; FIX = **silent seeding** (Cells.PlaceSilent/FillTileSilent + FillSkyTilesWithin now fills the sea SOLID — porosity was an artifact, band is the contract) + **WakeFreeSurfaces(band)** (only cells w/ an open cardinal neighbour start awake; interiors born asleep = their steady state; ReadState resume same treatment). PerfTest scenario now mirrors the real path incl. the sea: pre-settle 4862→441ms. 0-fps overhead = cell draw stride ladder was zoom-absolute (tuned for 1px cells) → now derived from screen-px-per-cell (`cellPx = zoom*TileSize/Density`, stride ceil(1.4/cellPx) cap 8) at both Draw+AddLights sites. TRAP: don't "restore" the random sea fill — the 63% porosity recreates the collapse stall.
- Round 8: rain = STREAMLETS (EmitRain emits 3 grains per drop: bright white-lerped 0.55px head + two 0.45px tail grains strung 2.2px apart, tail 4-8% slower so the string stretches in fall — the hose grammar pointed down).
- Round 9 Noita-fire pass: **hard 4-step palette fade in Particles.Draw** (`t = ceil(t*4)/4` — ALL particles cool in discrete ink jumps, no smooth lerps); flame jet grains pick from a 4-tone fire ramp + 5 BUOYANT "lick" grains/puff seeded along the tongue (GravityScale -0.5, curl UP off the stream — fire is buoyant, only the fuel payload droops; mid-air seeding correct for flame, wrong for acid); acid = 3 liquid inks incl. occasional deep green (liquids want dark grains, fire wants none); **ColorFor(Fire) tiered by flame-body position** (base w/ no fire below = white-hot, mid = gold-orange, tip w/ open above = deep red — burning pools read as licking tongues; flying embers' world-coord hash is bounds-guarded).
- Round 10 (user: "dots not streams"): **velocity-aligned motion smear in Particles.Draw** — grains >60 px/s draw as a rotated streak (~1 frame of travel, cap 6×size); overlapping smears fuse hose grains into continuous rope strands (a stationary square per grain reads as sprayed dots at ANY emission density); hose cones tightened (flame 0.24→0.17, acid 0.22→0.16 rad) + speed band 0.75-1.25→0.85-1.15 so smears bunch; flying-cell streak cap 4→6 cells. Rain/sparks inherit the smear (they're fast) — bonus streaking.
- Round 13 (user: "lags dropping from mothership + on landing"): the DESCENT runs the direct render path (fractional zoom lerp 0.72↔4, pixel-grid off) and the stride ladder still gave stride 1 down to zoom 1.8 → >1M cell candidates/frame mid-drop; FIX = direct-path stride kicks in below cellPx 1.6 (`_pixelK>0 || cellPx>=1.6 ? 1 : ceil(2.2/cellPx)` cap 10) + **AddLights stride floored at 2 everywhere** (seeds combine by MAX into 4px light cells — every-2nd seeding identical, halves the duplicate scan) + **interior LOD cut loosened stride>1→>2** in Draw/AddLights (stride 2 now occurs CLOSE-UP via fight-zoom/light-floor — the old cut would have vanished deep cells/lights in titan fights). Orbit over ember lava sea: 60fps Debug (drw 6.5ms). Ember ground: ~60 typical, dips to 40 in heavy fire scenes (Debug). Merged to noita-sim (2663974).
- Round 18: **rain decoupled from hose smear** — new `Particle.SmearMax` (world px, 0 = shared 16×size default) caps the motion smear per grain; EmitRain pins 3.3f (the original round-10 streak length, before the two hose-driven smear bumps). Future hose smear tuning must NOT touch rain — it has its own cap now.
- Round 20: rain SmearMax 3.3→6px + all rain grain colours ×0.72 (translucency = the "blur"; premult Color* scales alpha), fade ×0.4→0.3.

