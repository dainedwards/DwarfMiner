# Noita sim: cells, creatures, weapons, liquids

<!-- Folded out of Claude's memory into the repo 2026-07-16: CLAUDE.md is the single
     source of truth and links here. Notes are dated and historical by nature — trust
     the code over any line here, and correct the note when you find it stale. -->

Noita-feel pass landed 2026-07-13 on branch `noita-sim` (auto-committed by the parallel session's hook, so no single feature commit).

- **Flying cells** (Cells.cs): grid cells launched with full 2D world velocity, integrate under planet gravity, re-enter grid via TryDeposit/Place on contact. Mass-conserving (`LaunchCell`/`EjectFromTile`); `LaunchAtWorld` is emit-only (fire embers). Transient — dropped by WriteState like kinetics. Cap `MaxFlying` 4000; drawn as velocity streaks in Cells.Draw (stride==1 only).
  - GOTCHA/FIX (2026-07-14): the only speed cap was `FlyMaxSpeed=520`, so an outward-launched grain arced ~75 tiles into the sky under FlyGravity and hung near its apex as a lone 1-px "floating pixel in space", always on the side the player fired/dug. Fix = `FlyMaxOutward=170`: `UpdateFlying` bleeds off the radial-outward velocity component each tick (before the move, so even a huge explosion impulse is capped step 1). Keep this — reverting it brings the floating pixels back. Lateral/inward motion untouched, spray still arcs ~8 tiles.
- **Feeders**: CarveCrater ejecta (budget 90 cells, inner debris fastest) + fire seeds in vaporised core; TickLiquid splash (impact > TerminalCells/2, 1/3 chance, needs air above); QuenchIfWet gravel spark; Game1.OnTileBroken dig spray toward the dwarf.
- **Fire (mat 10)**: probe-driven — water quenches to steam, ignites adjacent oil/gas 1/3/tick, chars Grass/Support/Ladder 1/50, decays 1/42 fuelled vs 1/8 starved, flicks upward, spits embers. Gas ignition (lava) now yields Fire, not smoke → flame fronts. Fire wakes neighbours on TryMoveTo arrival (sleeping oil pools must catch).
- **Oil (mat 11)**: lighter than every liquid — buoyancy swap at top of TickLiquid (denser cell sinks through oil below). Seeded via Planet.OilSeeds / PlanetDef.SeedsOil (debug, verdant/living, slag; NOT ember — gas band is deeper, oil 14–42 depth) filled in Game1.BuildSessionWorld.
- **SampleHazardsNear returns 4-tuple** (lava, acid, gas, fire); player FireBurnDps 14.
- Tests: SimTest `TestNoitaFeel` — pockets must be carved by ANGLE (`Ti(r, angFrac, da)`), tile counts vary EVERY ring (chord ≈ TileSize, no bands), constant tile index skews and walls leak.
- **Creature env damage** (same day): Creature.Update probes SampleHazardsNear every 0.2s (random phase stagger) — lava 26 dps + burn, acid 12, fire 5 + burn, filtered by ImmuneTo (Fire now aliases the lava-immune kinds). Deliberately NOT wired to HitFlash (it doubles as digger provocation). Titans untouched (kaiju shrug hazards); no drowning for beached/landlocked creatures.
- `hazard: acid melts granite but obsidian resists` became flaky AFTER the splash change (droplets sped acid drainage from its unwalled pocket) — fixed by walling the test in obsidian, carved by angle. Verified base commit 3/3 vs branch 2-in-5 fails before the fix.
- Perf after change: meteor aftermath mean 0.40ms, worst 16ms — no regression (see [performance](performance.md)).

2026-07-14 combat/movement pass:
- **Bandit enemies** (Creature.cs, enum tail: Marauder/Raider/Pyro): humanoids with WEAKER player weapons. Marauder = ground gunner (TitanShotKind.**Slug** — new enum member, small tracer, dmg 6, knockback 60); Raider = jetpack hover + 3-round SMG bursts (`_burst/_burstT` fields, drawn pack flame); Pyro = closes to 95px and hoses REAL Fire cells via `cells.LaunchAtWorld` (ImmuneTo Fire but NOT Lava). All share `_gunAim` field driving the drawn weapon. Spawn via cave census bands (surface Marauder 6%, mid Marauder+Raider, deep Pyro+Raider); corpse drops iron/coal/fuel.
- **Flamethrower/acid spewer**: 2x slower cadence (cd 0.11/0.12), 6 cells/puff, wider fans, double per-hit damage to hold dps; Particles jets 10/9 droplets + smoke/vapour curls.
- **Melee rung-4 cut raymarches** from the player outward per arc angle and bites the FIRST solid tile ≤0.95 reach (was fixed 0.85-reach samples that skipped near blocks).
- **Jump/jet Noita retune**: JumpSpeed 118, air accel 220, JetLift 150, rise caps 70/82/95/110; **tap-vs-hold** — `_jumpHoldTime > 0.2s` gates the jet, so taps just jump; **W/Up removed from jumpHeld** (Space only; W still climbs/swims, rocket-ascent thrust untouched).

2026-07-14 air/water/sea-monsters pass:
- **Unified AIR meter**: Breath meter DELETED — merged into Player.Oxygen (the one "AIR" bar). Game1.TickAir replaces TickOxygen+TickBreath: drains at `AirDrainPerSecond` 8/s when `HeadInWater && !HasGills` OR `Def.Airless && !HasHelmet`; else refills (OxygenRules.RefillRate). `HasHelmet` = has the vacsuit upgrade (the pressure suit's sealed helmet → airless worlds breathe). `EffectiveMaxOxygen` now folds in lung tiers (×1.5/×2) alongside air-tank/O2 — lungs raise the AIR ceiling. Removed EffectiveMaxBreath/BaseMaxBreath/Breath field + DrownDps. HUD: one AIR bar (Renderer.DrawHudBars — breath bar block gone; AIR tints deep-blue while HeadInWater). RunSave unchanged (only Oxygen was ever saved). SimTest lung test → checks EffectiveMaxOxygen x1/1.5/2.
- **Deeper ocean water**: WorldGen lake depth gets ×1.6 when `def.LakeScale > 2.5` (ocean worlds) — seas plunge into real deep water WITHOUT widening more (width still only scales with LakeScale, so the surface isn't flooded further).
- **Hostile sea monsters** (Creature.cs, IsWaterKind extended): **AlienShark** (fast torpedo hunter, MoveSpeed 96, TickShark hunts a swimmer in-water else patrols), **Gulper** (deep anglerfish, glowing green lure via AddLight, TickGulper drifts then LUNGES 3.2× speed with `_lungeDir`), **Brinespitter** (aquatic artillery — TickBrinespitter lobs TitanShotKind.Acid globs as water slugs from ≤240px). Shared **SwimToward** helper (sets Velocity only — the Update substep integration moves+collides; must NOT touch Position). Spawn via SpawnDirector.**LakeKindFor** (deep water ~half hostile: whale24/shark26/gulper18/spitter14/crab; shallows shark22/crab). Draw cases + corpse drops added. SimTest "aquatic: shark hunts a swimmer" (40→0px).
- **Gems reshaped** (user: smaller, no protruding tips, more diamond): Renderer.DrawGemCrystal AND Game1 loose-Pickup draw both rewritten to ONE clean compact diamond = rimmed rotated-square (rim 2.2-2.6px, body 1.5-1.9px) + facet + glint. Dropped all the `pos ± axis*N` protruding shard/tip rects and the elongated-shard/crown-facet variants. Loose pickups keep the travelling shine.

2026-07-14 big combat/city/tools batch:
- **Pickaxe**: cooldown uniform 0.24 at ALL tiers (was tier-1×1.5) — upgrades add STRENGTH (EffectivePickaxePower=tier), never speed.
- **Jetpack/jump SPLIT keys**: Player.Update now takes `jetHeld` separately. **W (or Up) = jump**, **Space (airborne) = jetpack** (W never lights the pack). Reduced jet accel (JetLift 110, JetInitialKick 35, rise caps 62-95). All Player.Update callers updated (+false).
- **Scanner is ACTIVATED** (not passive): "scanner" is a usable belt item (crafting grants it + ScannerTier=1, Use=DoScanPulse). Firing pulses (2.5s cooldown) → snapshots nearest deposit per detectable material within a tier-scaled radius (200→560px), marks each with an expiry = now + 15×tier seconds (up to 60s). `_geoScanHits` now (kind,pos,expiry); prune in update; expanding dotted ring drawn in the WORLD pass (NOT the screen overlay — that crashed: no active batch). GrantGodmodeItems adds the scanner item.
- **Energy blaster** (renamed nuke→"ENERGY BLASTER", item id stays "nuke"): CHARGE-UP weapon (FireEnergyBall). Ball visual (fired projectile Radius + charging muzzle orb) is ×0.35 of the original at every charge stage. Hold LMB (isEnergyBall branch, parallel to throwables) → _energyCharge 0→1 over 1.6s w/ rising hum + growing muzzle orb; release fires. **Non-linear** damage/size/blast/AlloyMinePower via `EnergyPower(c)=Pow(c,1.74)` (half=30%, full=100%). New alien-cannon held sprite + BuildNuke icon. Projectile draw = pulsing violet orb w/ orbiting sparks.
- **Alien metal blast resistance**: Projectile.AlloyMinePower (default 2) — CarveCrater uses it; Nuke sets 12 so a full energy ball breaks alien alloy/glass/brick in ~4 hits.
- **City**: saucers 4→6/district + ONE **BigSaucer** command ship per city (CreatureKind, 900hp, TickSaucer patrol; Game1 militia block fires a 3-laser fan + a constant tractor beam that reels+slows the player; draw = big hull w/ 3 belly turrets + beam cone). Civilians 3× (cluster 2-3 per address, budget 260, AlienHome cap 3→8). Non-civilian aliens 4× HP (Lizardman 120, Peacekeeper 208, Saucer 176). **Any alien kill aggros the city** (resident-kill wrath 35→60, over the 50 threshold; now includes Lizardman/BigSaucer). Tower fix: doors on BOTH sides at street level (dropped the doorSide gate), ladder shaft moved to CENTER column (was edge) full-height — CityProbe doors 180→360.
- **Weapons 30% smaller** (melee draw scale 1.37/0.91→0.96/0.64, guns 0.55→0.39) + clearer swing (smootherstep-eased arc + 3 fading motion-trail afterimages).
- **Trees/wood** (task): new `wood` material (harvestable, bankable). TileKinds TreeTrunk(50, drops wood×2)/TreeCanopy(51)/TreeCanopy2(52, violet)/SeaFrond(53). WorldGen.ScatterTrees (TreePlanFor density by biome: verdant 26 / ocean 20 / frost·crystal 12 / acid 7 / ember 5 / **slag 4 barren** / **airless 0 no wood**) — trunk column + canopy blob on walkable soil; ScatterWaterPlants (SeaFrond on lakebeds). Trees/plants are **NON-anchored + IsFlora** (hazard-immune, titan-crushable — anchored plants wall the boss out like the old flora bug). FindSurfaceSpawn now skips IsFlora (spawn on ground, not tree tops). Physics settle skips IsFlora (they don't cave). **WorldGen.ScatterVegetation flag OFF in SimTest** (clean surface for titan/defense mechanic tests). Renderer authored-art per kind.
- **Item context menu two-step** (CharacterScreen): RMB item → EQUIP / UPGRADE > / DROP; clicking UPGRADE > opens the DETAIL (materials line from UpgradeInfo + CONFIRM/BACK) instead of applying immediately (`_ctxUpgrade` flag) — "see the cost before committing".

2026-07-14 blast + ladder pass:
- **Center-weighted blast falloff**: new `Combat.BlastFalloff(t)=Clamp(1 - t*t, 0.1, 1)` (t=dist/ExplosionRadius). Replaced the old linear `1 - 0.6t` (center 1 / edge 0.4) at BOTH sites — Combat.ApplyExplosionDamage (creatures) and Game1 player self-blast (damage + knockback). Now center full, edge 10%, squared drop. Titan blast still flat 0.4x (no falloff).
- **Tower ladder shaft fix**: the center ladder (dt==0) was a 1-tile hole through a 2-tile-thick floor slab — the flanking slab tiles (dt=+/-1) walled the dwarf in so floors above blocked the climb. WorldGen.BuildTower now clears dt in {-1,0,1} to Sky full-height (before the slab/furniture fill) → clean 3-wide open climb channel, ladder centered, alloy wall backing kept via SetWall. cityprobe Ladder ~2121.

2026-07-14 living-tree ecosystem (big pass, branch noita-sim):
- **Trees rebuilt tall/thin/varied**: WorldGen.ScatterTrees now plants 1-tile-wide SLENDER boles 6-16 tall in 4 species (0 spire/tallest, 1 broad/shortest, 2 umbrella flat cap on bare bole, 3 weeping drape). Trunk render narrowed to a ~3/8-wide bar (Renderer TreeTrunk case). Shapes live in TreeEcology.CanopyOffsets so gen + regrow build identically. Occasional off-tone canopy for variety.
- **New TileKind.TreeRoot (54)**: underground root threaded MaxRootDepth=4 below the ground tile (TreeEcology.PlantRoots), dark woody render, drops a scrap of wood, HitsToBreak 3. Roots survive felling; grubbing ALL of them out kills the tree for good.
- **Fell-to-dust**: Game1.ToppleTree (called from OnTileBroken when a TreeTrunk breaks) collapses everything above the cut — trunk tiles → full wood dust (SpawnDustInTile), canopy → 30%-of-a-tile foliage dust via new Cells.SpawnDustFraction(tx,ty,src,frac). Canopy/root now have Tiles.Drop=("wood",1) so foliage dust is pickable. Marks the matching TreeSite Standing=false, Growth=remaining.
- **TreeSite** (Planet.Trees list, Session.Trees => Planet.Trees; NOT serialized — TreeEcology.RebuildSites re-derives sites from TreeRoot columns on ResumeRun): Angle/GroundR/Species/Height/Canopy/Growth/Moisture/Standing.
- **Regrowth** (TreeEcology.Update, ticked in Game1 after AmbientDirector): felled+rooted trees regrow trunk ring-by-ring; rate 0.018*(0.4+moisture*2.8) so watered ≈6x parched. Moisture from rain (WaterBand over the cloud's angular band) + biome baseline (ocean/verdant 0.45, wet 0.22, dry 0.08) + SoakFromPools (nearby Water cells always, Acid/Fire cells only if that's the biome rain).
- **Weather** (new Weather.cs + Session.Clouds/CloudTimer + Cloud class): <=4 drifting clouds form near the player, fade in/out, drift, and rain in showers. RainKind by biome (TreeEcology.RainFor): Water default / Acid (acid biome) / Fire (ember,slag). Rain = Particles.EmitRain streaks (blue/green/orange) + WaterBand moisture; acid/fire rain also puffs a rare Gas/Smoke cell (NOT real acid/fire — never eats soil or ignites, per brief). Clouds drawn in Game1.DrawWeather (world pass, near acid-rain cloud). AcidRain DISASTER (harsh, real acid cells) is separate and unchanged.
- SimTest.TestTreeEcology: plant→trunk+roots, fell→gone-but-roots, water→regrows, grub roots→dies. ScatterVegetation still OFF in SimTest.

2026-07-14 big 15-item combat/world/ecosystem batch (branch noita-sim):
- **Jetpack tier1**: JetChargeMax 0.75→1.0s; JetInitialKick 35→62 (boost pop); reworked to on/off Noita thrust — Space lifts you straight off the GROUND (no W-jump first), W plays no part in flying. SimTest "jetpack burn" now 1/2/3/5.
- **Lava sparks 99% cut** (Game1.TickHazardContact: EmitImpact rate dt*20→dt*0.2) + set Player.HurtFlash instead.
- **Player damage indicator**: new Player.HurtFlash (set in TakeDamage, decays), Renderer.DrawHurtVignette = red screen-edge bands, drawn before HUD.
- **Fire on flammable surfaces**: Cells.IsFlammable now includes Fernleaf/Platform/TreeTrunk/Canopy/Canopy2/TreeRoot/SeaFrond (biome-adapted flora stays immune). Lava ignites them via new TryIgniteTile in TickLava. Global **fire-spread throttle** `_fireBudget` (max 90, regen 22/s, SpendFire()) gates NEW-tile ignitions + ember spits + lava ignition so fire can't cross a whole planet (oil/gas burning ungated). SimTest "fire gutters out" still passes.
- **Cave-ins easier**: Physics loose-ground now crumbles unless below-solid OR both inner shoulders solid (was: needs all 3 empty); StoneCollapseBudget 192→256.
- **Gems rarer + crystals rarest**: ambient gem thresholds pushed high (Ruby .985/Sapphire .988/Emerald .990/Diamond .992/**Crystal .994 = rarest**), thinning &3→&7 (1/8 sites). Still embedded via SetGem (crystal caverns/geodes left as landmark set-pieces).
- **Gold/silver on EVERY world** (incl. alien homeworld), rarer than iron: base thresholds reachable (silver .945/gold .955, was 1.05 bias-only); coarse `metalSel` noise splits crust into gold-country vs silver-country so they rarely mix. StampRichVeins unchanged. SimTest "gold reaches every world" updated.
- **Dust labeled by source**: Cells.SrcTileAt(cx,cy); hover readout shows "MATERIAL: <src> (LOOSE)" not "Dust".
- **Guns slower**: bullet/pistol/MG + gem cannons — lower projectile speed AND higher ShootCooldown. Bullets redrawn as small oriented rounds with bright tips, each gun slightly different (brass/gold/copper).
- **Arced creature projectiles**: new TitanShotKind.Dart (ballistic — Arcs, but doesn't Splash; Titan.cs Ballistic split into Arcs vs Splashes). Lizardman bone-spear → arcing Dart blowdart (lofted aim). AcidSpitter/Brinespitter already arced.
- **Less erratic jumping**: Creature._hopDir + IdleHopDir() (pick once, 1-in-5 reverse) for TickHopper/TickSlime idle hops; steadier cd variance.
- **3 new enemies** (Creature.cs, Noita-flavoured; enum tail Quillwing/Warpwisp/Thornback): **Quillwing** flyer fires a 3-Spike fan (reuses TickCaveEye); **Warpwisp** floating caster lobs slow wall-piercing Void hex bolts; **Thornback** ground beetle lobs high-arc Dart barb-mortars over walls (no LoS) + repositions. Full wiring: stats/tick/draw/census(mid+deep bands)/corpse drops.
- **Lizard villages**: warren halls cross-linked (i→i+2 tunnels + entrance→vault spine) for a connected network; **treasure chests** — new TileKind.Chest/ChestOpen (anchored, brass-banded art), placed on vault floor, E-to-loot via Game1.TryOpenChest/OpenChest (10-21 gold + a rare gem/silver, toast+sfx). Pack behavior: Lizardman shrieks CallingBackup when ATTACKED (HitFlash>0.12), not just on sight → Game1 war-cry rallies the warren. Spawn in pairs: LizardDoor spawner emits 2/trigger (cap 4), garrison 2-3/hall.
- **Trees taller/varied**: species heights bumped (spire 12-21, umbrella 11-18, weeping 9-15, broad 7-12) + 1-in-6 GIANT (+8-17).

2026-07-14 tree-toughness / red-flash / snow follow-up:
- **Trees break less easily**: Hardness(TreeTrunk) 2→4, and Planet.Mine scales trunk hardness up by tree height (`+min(4, TreeHeightAt(x,y)/6)`, so a giant trunk hits ~8) — new Planet.TreeHeightAt(x,y) scans Planet.Trees. Topple-on-cut kept (physics: flora doesn't cave, so the crown must collapse when the bole is severed).
- **Player red flash**: player sprite draw (both _playerSprite anim + _dwarfTex paths) tints toward red by Player.HurtFlash (Color.Lerp White→(255,45,45)*0.85). (Screen-edge DrawHurtVignette call REMOVED 2026-07-14 per user — damage read is ON the dwarf only now; the Renderer method is left unused.)
- **Snow on ice worlds**: RainKind.Snow added; TreeEcology.RainFor "frost"→Snow (the "frost" biome IS the ice planet, def already exists). Weather.Rain emits Particles.EmitSnow (slow drifting fluffy flakes, low gravity) instead of streaks, skips the gas/smoke puff; DrawWeather snow clouds pale-white. Snow still waters trees (WaterBand).
- **Test fix (not a regression)**: SimTest.RunSavePath() used a FLAT `.../DwarfMiner/run.sav` but RunSave.SavePath is slot-based (`SaveSlots.Dir(Active)/run.sav`); when a slot save exists, RunSave.Exists=true then the flat read threw FileNotFound and crashed the whole suite. Fixed RunSavePath() to use SaveSlots.Dir(SaveSlots.Active). (Exposed by leftover slot save from DM_AUTOSTART debug launches.)

2026-07-14 ore-balance / gem-embed / tree-polish follow-up:
- **Gold less common on rich worlds**: gold/silver OreBias values were tuned for the old UNREACHABLE base threshold; now the base is reachable everywhere so those biases flooded the crust. Slashed them — PlanetDef gold biases 0.13/0.125→0.05, silver 0.135→0.055, debug-world gold/silver 0.16→0.06, belt 0.13→0.06; PlanetGen sigBias gold 0.12→0.05, moon silver 0.14→0.055, rare-vein gold J(.07-.09)→J(.03-.045) silver J(.12-.15)→J(.045-.06). (verdant gold tiles 4648→1727.)
- **Platinum decorrelated**: WorldGen ore scatter samples a SEPARATE noise for platinum (`platN = SampleNoise(oreNoise, wx*.53+1234.5, wy*.53-987.6)`, threshold 0.978) so platinum veins no longer share seams with iron/gold/silver — its own isolated pockets.
- **Platinum recoloured** distinct from silver: base (206,198,172) warm champagne-pearl / glint (255,246,210), vs silver's blue-white.
- **Gems/crystals embedded, never solid blocks**: crystal-cavern inner shell (WorldGen geode + SeedBiomePockets) now lines with Stone STUDDED with SetGem(Crystal) instead of solid TileKind.Crystal tiles; warren vault centrepiece ruby is GoldOre + SetGem(Ruby). Ambient scatter already embedded. ("gems never generate as own tiles" test still green.)
- **Trees per biome**: new WorldGen.TreeSpeciesFor(biome) weights the 4 species so each world grows its own forest (frost=spires, verdant=broad/weeping, acid/ember=bare umbrellas, crystal=spires…). Replaces flat rng.Next(4).
- **Roots smaller + spread**: TreeEcology.PlantRoots = vertical taproot (trunk column, RootsAlive/RebuildSites signature) PLUS shallow lateral flare roots (depth 2, ±1-3 cols, sparse, via new TrySetRoot). RebuildSites now requires a TAPROOT signature (shallowest root that also has a root one ring inward) so lateral roots don't seed phantom sites. Renderer TreeRoot redrawn thin+wispy w/ tendrils. NOTE PlantRoots uses Random.Shared (cosmetic non-determinism; roots are saved as tiles).
- **Tree fidelity**: Renderer TreeTrunk gets hash-keyed bark grain ridges + occasional knot + sun/shade edges; TreeCanopy gets a layered under-shadow/body/sunlit-crown + 9 dappled leaf clusters in 4 tones.

2026-07-14 zoom / eruption / lizard / flame / gem-embed follow-up:
- **Zoom keys**: hold "-" (OemMinus/Subtract) to pull out, "+"/"=" (OemPlus/Add) to push in — adjusts `_playZoom` clamped [2.2, 8], set on _camera.Zoom each play frame (in the play update, just before _camera.Follow).
- **Debug/scheduled eruption picks NEAREST volcano** to the player (AmbientDirector.TryBegin Eruption loops VolcanoVents for min-dist), not a random vent. Duration bumped 5-9s → 8-14s.
- **Eruptions MUCH bigger** (were a 12-cell trickle): Game1 eruption tick now wells up a fat magma surge (3× SpawnInTile at Density² each = ~48 cells/frame that overflow the crater) AND SPEWS a fountain — 9-16 gobs/frame LaunchAtWorld'd up+out of the vent mouth (spread ±0.5rad, speed 150-210, arc over and rain lava down the flanks; note FlyMaxOutward=170 caps fountain height, that's fine) + smoke plume + EmitCinders + shake 0.4 + a low "collapse" rumble (minGap 0.5).
- **Lizard darts reworked**: fire a 3-dart BURST in quick succession (reuses `_burst/_burstT`, 0.13s apart), SMALL darts on a BIG arc (loft 0.42+dist*0.0026, speed 200, dmg 5 each), ~2.8s between volleys. **No more lunging/jumping** — TickLizardman now holds a standoff dart-range (back off <90px, close in >190px, else plant and shoot). The dart "explosion" was Game1's blanket `EmitImpact(Cannon)` on every dead TitanShot — now Dart/Slug/Spike get a small `EmitImpact(Bullet)` puff, only heavy ordnance (flame/acid/lava/void) throws the cannon burst.
- **Warren cave exits**: WorldGen.CarveLizardCities bores a short brick corridor OUT of each hall into the surrounding cave-riddled crust (CarveTunnel, reach 120-210px), gated by a 3-tall DoorClosed at the mouth. Lizardmen (CanUseDoors) open them and range into nearby caves — warrens aren't sealed.
- **Flamethrower/acid spewer**: TIGHT stream — spread 0.42→0.18 (flame) / 0.36→0.15 (acid), 6→3 cells/puff, cadence 0.11/0.12→0.055/0.06 so it reads as one steady stream not pulsing waves; per-puff dmg halved (52→26, 64→32; titan 80→40, 90→45) to hold dps.
- **Embedded gems blend into surrounding rock**: Renderer now renders a gem's host tile (embedded overlay OR legacy gem tile) as HostRockFor(neighbours) — the majority neighbouring rock — so a gem no longer reads as its own dark block set apart from the wall; it sits IN the surrounding material.
- NOTE: perf test "steady-state cell tick under 6 ms" flakes to ~25ms when the machine is CPU-throttled from many back-to-back `dotnet run` invocations (warm-up hit 249ms/tick — physically impossible from a code change). No edit this session touches Cells.Update or the perf scenario (plain WorldGen.Generate(7) + lava/water). All ~350 other tests + cityprobe pass.

2026-07-14 lizardman-aim / vignette / gold-depth follow-up:
- **Lizardman fixed**: moves more (tighter 105-150px hold band + moves to get an angle when LoS blocked, so it's not frozen); FACES the prey while hunting (Draw overrides `facing` from player.Position when _aggroT>0, and TickLizardman sets _gunAim at the player); shoots even POINT-BLANK and at LEVEL targets — fire min-dist 60→25, and the dart aim is now `dir + up*clamp(dist*0.0022, 0.05, 0.55)` (nearly flat up close, high arc only at range) with speed `190 + dist*0.4` so it carries before dropping.
- **Screen hurt-vignette removed** (see character-equipment note) — damage flash is the dwarf sprite only.
- **Gold/silver pushed deeper**: base thresholds 0.945/0.955→0.955/0.962, min depth 30/40→46/56, depth boost 0.6→0.85 — scarce in the upper layers, still findable deep. (verdant gold 1727→1526.)

2026-07-14 doors / hose-rework / clouds / deep-caves batch:
- **Doors one piece**: Renderer DoorClosed now draws a continuous leaf (stiles + panel lines full-height) with the brass handle drawn ONCE — only on the bottom tile of the run (innerK not a door) — so a tall door reads as one door, not stacked panels each with a knob. Functionally already toggled as a whole run (SetDoorRun). Aliens already open doors: CanUseDoors (Civilian/Peacekeeper/Lizardman/Marauder/Pyro) is wired into GroundMove.
- **Flamethrower + acid spewer reworked**: now a HELD stream that grows length the longer it's held — new Game1.StreamReach() (_streamHold ramps to StreamHoldMax=0.35s (was 1.1 — user wanted max length much quicker), reach lerps 42→132px; a >0.15s gap resets it, shared by both hoses). Particle jets take a `reach` param and set **CollideTiles=true** so flame/acid/smoke splash on walls instead of ghosting through them (EmitFlameJet/EmitAcidJet + hero-flicker + cinders all reach-scaled + collidable). Cells launched at reach*2.1 speed, 3/puff, spread 0.18/0.16. Cooldown 0.05, but the "machine-gun" per-shot sound fixed: flamethrower/acid_spewer play their ShotSound on minGap 0.32 (a sustained low roar) instead of 0.03 — see the shot-sound block in Game1. Noita-look pass (2026-07-14): particles ARC WITH THE CELLS — `HoseArcGravity = 2.25` (cells pull FlyGravity≈450 px/s², particles 200×scale → 2.25 matches) replaces the old rise/no-arc scales, so the visible tongue droops exactly onto where the payload lands; 9 blobs + 3 smoke/vapour curls per puff (was 5+2), spread 0.24/0.22, bigger sizes (hot 1.8 / body 2.4-3.2), smoke curls seeded ALONG the tongue (pos + d*reach*0.35) that buoy up after.
- **Clouds ride above terrain**: Weather.TargetAlt samples 5 bearings across the cloud's band, finds the topmost non-sky tile (peaks + giant trees), and sets the cloud ~130px + random clear above the tallest; the cloud Alt eases toward that target each tick as it drifts (rises over mountains, settles over flats).
- **Cave networks reach the lower depths**: CarveWormTunnels minFrac 0.50→max(0.38, LavaFillFrac+0.08) — worms thread the lower crust now, still a safe margin above the lava fill line.
- **Rare minerals = smooth depth gradient** (replaces hard depth gates): new depthFrac (0 surface→1 deep) + a shallow threshold PENALTY (metals 0.075, gems 0.06). Gold/silver base 0.948/0.955, gems 0.982-0.994, low depth floors (12-20) so they're RICH deep, some in the mid crust, and very rarely in the upper layers. (verdant gold 578, emerald 8 — tests green.) Crystal still rarest.
- **SimTest**: delver site now also requires solid ground BELOW (the deeper caves left open space under some sites → delver fell instead of digging).

2026-07-15 lush-worlds pass:
- **Clouds over water fixed**: Weather.TargetAlt clamps the "tallest thing" to at least the baseline surface ring — over lakes/oceans the topmost-solid scan found the LAKE BED (water is cells, not tiles), parking clouds underwater on deep seas.
- **TileKind.LilyPad = 57**: anchored (floats, never falls) + IsFlora + passable, pad-green, authored render (bobbing pad w/ rim notch, vein sheen, 1-in-4 luminous violet blossom w/ pulsing amber anther). ScatterWaterPlants rework: seaweed rooted at frondPct (ocean 24 / other wet 12) in STACKED 1-3 kelp columns (stay under waterline via basin set), lily pads on the surface row (basin tile w/ sky above; ocean 22 / other 10).
- **More vegetation everywhere**: ScatterBiomeFlora 35%→48%; FloraFor "ocean"→Fernleaf (was none); TreePlanFor verdant 26→34, ocean 20→30, frost/crystal 12→16, acid 7→10, ember 5→7, slag 4→6, city 10→14, default 12→16.
- **Oases** (WorldGen.ScatterOases, rng seed^0x0A51, inside ScatterVegetation block): gentle worlds only (Difficulty<=0.35, not airless), 2-4 sites, half-span 6-10 tiles — a tree every 3rd tile (h 9-16) with FloraFor undergrowth carpeting every other ground tile (Fernleaf fallback so even ocean/city oases have groundcover).
- **CONCURRENT-SESSION NOTE (2026-07-15 ~01:15)**: the parallel session landed a `WorldGen.CaveStrata` lava-sea rework (Game1 lava seeding now `FillSkyTilesWithin(..., seaFloor)`; "sealed dry strata" deep cave layers) MID-FEATURE while this session tested. Suite failures at that moment — city districts span 17%, provoked-borer, rallied-guard closes, fire-breath 0 grains, "ordinary worlds stay round (verdant swings 14 rings)" — are terrain-shape regressions from THAT work, not the vegetation batch (SimTest runs ScatterVegetation=false; ecosystem/gems/acid all green). Machine also heavily contended (perf 82-89ms/tick, user playtesting). Re-check after the other session settles.

2026-07-15 biome-tree identity pass:
- **Trees per biome look DIFFERENT now**: Renderer gets `public string TreeBiome` (Game1 sets `_renderer.TreeBiome = _run.Def.Biome` right before DrawWorld — tiles stay one TileKind, only paint differs, save-safe). New `TreeBarkFor(biome)` + `TreeLeafFor(biome, alt)` palettes: verdant warm timber + green/AUTUMN-AMBER alt; ocean driftwood + sea-green/turquoise; frost dark pine + blue needles/icy pale + SNOW cap row + icicles + rime on bark; crystal glassy stalk + amethyst/sapphire + white facet glints; acid bile wood + chartreuse/olive + toxic drip + bark weep; ember charred coal + smoulder-leaf/ash + PULSING EMBER CRACKS in trunk + glowing buds; slag scrap-post + oxidised-copper/rust + bolt bands; city grey ornamental + teal/magenta + pulsing cyan biolume nodes; default alien mauve/teal/violet. Canopy2 = the biome's SIBLING SPECIES colour (not a tint).
- **Terraria proportions**: straighter (grain 1/4 sparse tick, knots removed) + taller — spire 16-27, umbrella 14-23, weeping 12-19, broad 10-16, giant +10-21 (worst ~48). TreeEcology.RebuildSites trunk scan 30→52 to measure resumed giants.

2026-07-15 fire-vs-snow / water-quench / gem-bed pass:
- **Flying grains pass through gas cells**: UpdateFlying — Smoke/Gas/Fire cells no longer block a flying grain (IsBlocked counts ANY cell mat, so the steam curling off an acid splash was parking the next spurt mid-air → acid stacked in the sky and the stream dropped short). Pass-through gated on !SolidTileAtCell (new helper); a flying Fire grain passing a Gas cell still ignites it (IgniteCell).
- **Fire melts snow**: TickFire probe — a flame beside a Snow tile (1/6 per probe, NOT fire-budget-gated) melts it to Sky + Water(Density/2) + steam; the meltwater then douses the flame naturally. Lava already melted snow via IsMeltable.
- **Fire lingers + catches better**: gutter roll fuelled 42→56, starved 8→16 (~0.9s/0.27s); flammable-tile char rate 1/50→1/28 (still SpendFire-gated).
- **Flying fire quenches on water**: UpdateFlying contact branch — a flying Fire cell whose blocking cell is Water flashes to a Smoke wisp at its last free spot (never banks burning cells on/under pools). FireFlamethrower with the MUZZLE in water just places a Smoke bubble and returns (can't light underwater).
- **Acid vapour rain fixed**: EmitAcidJet wisps were seeded mid-air along the rope (pos + d*reach*0.35) and fell like rain — now 2 wisps seeded AT THE MUZZLE riding the rope's arc (HoseArcGravity*0.8). Flame smoke curls stay mid-tongue (reads as smoke, user didn't flag).
- **Gem drops sit in host sand**: SpawnDustInTile gem-tile branch now ALSO fills the tile with dust tagged with a neighbouring host rock (inner/left/right, first solid non-gem non-anchored, default Stone) — gem pickup rests in a bed of the material it was dug out of, not a black hole. (Embedded-overlay gems already dusted their host.) TestGemDrops updated: "leaves a bed of host-rock dust". Pickup draw: dark rim (body/2, 2.6px) → thin Lerp(body,black,0.35) outline at 2.2px — the old rim read as a black square backing.

2026-07-14 acid-depletion / granular-hose pass:
- **Acid spends itself eating**: TryCorrode now returns bool; TickAcid, when any neighbour bite landed, fizzes the acid cell itself to Smoke 2-in-3 — each cell eats ~1.5 tiles then is gone (a spewer puff ≈ 4-7 tiles, no more ever-deepening pits). Contained world pools (obsidian-lined) unaffected — nothing to eat. All acid sim tests still green.
- **Granular hose pixels**: EmitFlameJet/EmitAcidJet blobs shrunk to ~0.8-1.5px (was 1.8-3.2) and count bumped 9→14 (+smoke/vapour 3→4, also ~1.1-1.6px) — many tiny grains = Noita pixel-fluid look, same arc/collide/reach behaviour.

2026-07-15 rain-pooling / slow-regrow pass:
- **Rain water is REAL now**: Weather.Rain drops throttled (1-in-3/frame) Water cells 4-14px above the surface under the band via new `Cells.SpawnRainWater(worldPos)` — rain pools in hollows instead of ghosting through terrain. Water-kind rain ONLY (acid/ember stay atmospheric per brief, snow stays visual).
- **Rain-water provenance = `_srcTile` 255** (`RainWaterSrc`, water never uses its src slot): rides every move/swap/launch + the save. **Evaporation** in TickLiquid (before the hemmed-sleep clause): rain-marked water exposed above (new `ExposedAbove` = any outer-row cell unblocked) evaporates 1/600 per tick (~10s once exposed) — puddles dry top-down, seeded lakes/oceans NEVER evaporate. TRAP: a flat 1-deep puddle is hemmed by its own row-mates and would sleep past its roll — the hemmed branch re-enqueues rain-fed exposed cells (buried rain cells sleep like lake interiors and just become lake).
- **Rain particles collide** (EmitRain CollideTiles=true, life 1.0-1.6 since impact now ends them) — drops splash on the surface instead of streaking through the ground.
- **Trees regrow ~4.5× slower**: TreeEcology rate base 0.018→0.004 (several real minutes even watered), per user.

2026-07-15 fine-pixel-fidelity 10-pack (user: "do all those including bigger swings"):
- **Material.Snow = 12** powder: rides TickSand (SlipChance 70), IsCompactable → compacts to TileKind.Snow (MajorityKind). `Cells.SnowPersists` set by Weather EVERY frame from biome=="frost" (headless defaults false). Non-frost: exposed resting flake sublimates 1/900/tick (stays awake only while ExposedAbove; buried flakes sleep). **SpawnSnow saturation cap**: skip if CountNear(pos,8,Snow)>20 — snow has NO evaporation counterweight on frost, this cap is the mass bound AND keeps blankets too shallow to compact. Weather deposits snow cells like rain (1-in-3/frame under snowing cloud).
- **Snow melts to RAIN-TAGGED water** both fire-side (TickFire probe `case Material.Snow` 1/2) and lava-side (QuenchIfWet falls back to FindNeighbour(Snow) when no water) — slush dries via the rain-evaporation path.
- **Damp ground tint**: `Renderer.WorldCells` (set by Game1 beside TreeBiome each frame; null elsewhere skips). Tile loop probes cell row `(r+1)*Density` (2 Gets) for water on the tile's outer face → ×(0.72,0.78,0.92) folded into BOTH `col` (flat/authored/crust) and `tintF` (atlas path — col alone does nothing for atlas tiles!).
- **Ambient sweep** (Cells.Update, 0.5s, 36 samples in 340px disc, gated on CompactionExclusion.HasValue so ALL headless tests skip it): (a) **ceiling drips** — open-air sample walks outward ≤12 cells to a bare ceiling; IsPorous kinds (Dirt/Grass/Gravel/MossStone/Conglomerate) with ≥3 water cells above the tile weep a rain-tagged droplet 1/4 (mass-duplicating BY DESIGN — draining would empty lakes through their beds; evaporation is the sink); (b) **moss creep** — water sample walks inward ≤3 to its bed, plain Stone→MossStone 1/30 (MossStone is IsLoose — mossy floors crumble when undercut, accepted).
- **TickSmoke seeps sideways under roofs** (same 2 lines as gas) — cave fires fill chambers ceiling-down and plume out openings.
- **ShedBurningLeaves** (both TryIgniteTile + TickFire char): leafy kinds (canopy×2/Fernleaf/SeaFrond/Grass) launch 2-3 falling flying-Fire embers — canopy fires rain glowing motes that can spot-fire or die in steam.
- **Bubbles**: Cells.PendingBubbles (cap 200, QueueBubble at quench-boil + flying-fire-drowns) drained by Game1 → 3×EmitBubble; player drowning exhales bubble trail in TickAir (underwater branch, dt*2.5 roll). EmitBubble = negative GravityScale rises along local up.
- **Falling leaves**: Weather.Update rolls dt*2.5, random standing TreeSite within 0.3rad of player, emits EmitLeaf at crown in `Renderer.TreeLeafFor(biome, canopy2)` colour — TreeLeafFor made PUBLIC for this.
- **Lightning conduction**: `Cells.ZapWater(pos)` BFS ≤700 connected water cells → transient `_zapped`+`_zapUntil` 0.22s (ColorFor water flashes electric white-blue; NOT serialized), returns every-90th positions for arcs. `ZappedAt(pos)` ±2-cell test. FireLightningGun: ray-marches for water BEFORE rock grounding (pool beats rock; closer creature beats pool), chain victim standing in water dumps the arc into its pool; ONE conduction per shot; zapped creatures 16dmg, PLAYER 10 via TakeDamage — never stand in the pool you shoot.
- **Creature WetSeconds/OilySeconds** (beside Burn/FreezeSeconds): set in the 0.2s hazard probe via CountWaterNear / new generalized `Cells.CountNear(pos,r,mat)`. Water-natives (ImmuneTo Water) NEVER get wet (would tint permanently). Wet 3s: douses burn, blocks ignition, halves fire dps; water rinses oil. Oily 8s: fire/LAVA contact burns 5s instead of 1.5. Tints in Draw's `Tinted` (oily dark 0.4, wet blue 0.25) below hit/freeze/burn priority.
- Rain particles: EmitRain got CollideTiles earlier same day; EmitLeaf also CollideTiles (leaves settle briefly).
- Suite: all green ×2 runs EXCEPT `rigid: mountain-scale slab breaks into several boulders` — that's the PARALLEL session's rigid-bodies-v2 test (added 01:59, files touched 02:37, mid-feature TryDetach signature race broke the build for ~10 min); `hazard: acid dissolves a soft tile` flaked once, passed 2/2 after.

2026-07-15 effects-as-materials — IN WORKTREE `cell-effects`, NOT merged (user wanted it isolated for testing; worktree at `.claude/worktrees/cell-effects`, branched off noita-sim; auto-committer commits there too, so no single feature commit):
- **Explosion smoke = real Smoke cells**: CarveCrater stamps `smokeBudget = min(150, 30 + tiles*10)` Smoke cells (3/tile) into the VAPORISED core; plume rises/pools/decays via existing TickSmoke (~3s gutter). Airburst (nothing carved) stamps a 10-cell epicentre puff via new `Cells.StampAtWorld` (= PlaceAtWorld but refuses cells inside solid tiles — particle rest positions can sit a hair inside a wall's cell footprint).
- **Cinder→fire handoff**: `Particle.LandMat` byte (Material stamped where a CollideTiles particle comes to REST, cleared after one stamp; chance rolled AT EMIT — EmitCinders gives 1-in-4 coals Fire). `Particles.Update(dt, planet, cells?)` — Game1 passes `_run.Cells` at all 7 sites; PerfTest/headless callers omit it. So every explosion/flamethrower/volcano-vent cinder can spot-fire where it lands (sim's own fire budget throttles spread).
- **Particle smoke demoted**: EmitExplosion smoke loop → ≤4 brief small puffs when CellFx; nuke's 14-particle rising column gated out entirely (its big crater budget covers it).
- **`DM_CELLFX=0` reverts everything** (static `Particles.CellFx`). **`--smokeprobe`** (SmokeProbe.cs + Program.cs arm) verifies: ground burst ≥20 cells + plume rises + decays to ~0 by 8s, airburst 1..10 cells, cinder handoff ≥3 concurrent fire cells. All green first run.
- Not done here (deferred): draw-side pixel polish (raster snapping, quantized fades) — discussed; EmitDust ambient sites (titans/corpses/lander) deliberately left as particles.
- **Noita-grain pass (same worktree, user: "small and plentiful, too chunky")**: EVERY Particles.cs emitter retuned — sizes ~0.7-1.4px (were 1-7), counts ×1.5-3 (explosion fireball strength*0.6→*1.8, hammer 14/10→26/22, dust 14→30, rocket exhaust 10→26, hoses 14→22, muzzle sparks 4→8…); explosion flash square strength*0.55 (14px on a nuke!) → min(3.5) heart + one-frame 1px white starburst, muzzle flash quad 3→1.6 (hero LIGHT is the flash); **lights moved to a SUBSET of grains** (every 2nd/3rd/4th) so tripled counts don't flood the lighting pass; Weather rain/snow 5→9 drops/tick at ~1px. Suite + smokeprobe green. Round 2 (user: "still too large"): **hard 1-world-px cap in Particles.Draw** — the ONE choke point, `min(Size, 1f)`, same grain as a sim cell; emitter Size>1 now only means "full pixel" (flash/exhaust hearts flatten to 1px; their job is the hero light). Any future "particles too big" report: the cap is in Draw, not the emitters. Round 3 (user: "still large — acid/fire/water/sand"): culprit was the CELL draw, not particles — Cells.Draw padded every quad +0.5px (seam bleed for rotated polar quads), so every isolated cell was a 1.5×1.5 square; fix = **neighbour-aware padding** (stride-1 only: bleed 0.5 per axis only toward IsBlocked neighbours — pools stay seamless, ground-bleed invisible; open-air sides 0.1 → lone droplet/ember/grain = crisp ~1px) + **area-conserving flying streaks** (width = 1/len, floor 0.55, so smears stay one grain of ink). Verified: lake body seamless, splash droplets pixel-crisp. Round 4 (user: "still too large/chunky"): ROOT CAUSE finally named — the terrain ATLAS (16×16 over 4px tiles) gives the world an apparent grain of ~0.25-0.5px, so even a true 1px cell quad reads 2-4× chunkier than the ground; fix = **EXPOSED cells (any open-air side) draw as 2×2 half-px sub-grains** w/ hashed shade 0.82-1.0 (pool/pile interiors stay single padded quads — quad budget bounded, sim untouched), flying streaks base 0.7px w/ area conservation, particle cap 1f→0.85f. The dynamic-matter grain ladder is now: cell skin 0.5px sub-grains / flyers 0.7px / particles ≤0.85px, all under the tile's 4px. Rounds 5-6: skin-ink/scatter/glint continuity change tried and REVERTED per user ("weird") except fire; ROOT-CAUSE trio then landed (user: "think hard"): (a) default `_playZoom` was 4.0 — effects drawn 4×4 screen px; (b) world res per character is half Noita's; (c) no pixel-grid discipline. FIX = **Density 4→8** (0.5px cells, dwarf ~18 grains tall; RunSave v24 — v23 reserved by titan pass; CompactMinFill was flat "-2" and silently tightened → now Density²/8 fraction; oil test 48-cell counts + perf bound 6→16ms Debug were Density-hardcoded) + **pixel-grid world RT** (Game1._worldRt: world layer renders at cam zoom 2 = 1 texel per 0.5px cell into Virtual/k target, ONE point-sampled integer upscale after EndEntities, lighting composites at native after; zoom keys now STEP {2,4,6,8} edge-triggered; landing/orbit non-step zooms fall back to direct path; DM_PIXELRT=0 disables) + sub-grain draw split REMOVED (cells fine enough now), pads made relative, flyer streaks in cell units, particle cap 0.55px. **Fire kept the dance** (per-grain 12Hz hash jitter ±0.22 in Cells.Draw) + **starved fire pools ~0.8s** (gutter 16→48). Perf at D8: Release meteor mean 2.65ms (was 0.4), worst 48ms spike (wake-storm class), Debug steady 13ms — watch for hitches in playtest. Suite PASS. Round 7 (user: "long load, 0 fps overhead"): load stall = seeding woke EVERY cell (sea+lakes ×4 at D8) then the porous sea (SpawnInTile random-with-replacement = accidental 63% fill) collapsed sea-wide; FIX = **silent seeding** (Cells.PlaceSilent/FillTileSilent + FillSkyTilesWithin now fills the sea SOLID — porosity was an artifact, band is the contract) + **WakeFreeSurfaces(band)** (only cells w/ an open cardinal neighbour start awake; interiors born asleep = their steady state; ReadState resume same treatment). PerfTest scenario now mirrors the real path incl. the sea: pre-settle 4862→441ms. 0-fps overhead = cell draw stride ladder was zoom-absolute (tuned for 1px cells) → now derived from screen-px-per-cell (`cellPx = zoom*TileSize/Density`, stride ceil(1.4/cellPx) cap 8) at both Draw+AddLights sites. TRAP: don't "restore" the random sea fill — the 63% porosity recreates the collapse stall. Round 8: rain = STREAMLETS (EmitRain emits 3 grains per drop: bright white-lerped 0.55px head + two 0.45px tail grains strung 2.2px apart, tail 4-8% slower so the string stretches in fall — the hose grammar pointed down). Round 9 Noita-fire pass: **hard 4-step palette fade in Particles.Draw** (`t = ceil(t*4)/4` — ALL particles cool in discrete ink jumps, no smooth lerps); flame jet grains pick from a 4-tone fire ramp + 5 BUOYANT "lick" grains/puff seeded along the tongue (GravityScale -0.5, curl UP off the stream — fire is buoyant, only the fuel payload droops; mid-air seeding correct for flame, wrong for acid); acid = 3 liquid inks incl. occasional deep green (liquids want dark grains, fire wants none); **ColorFor(Fire) tiered by flame-body position** (base w/ no fire below = white-hot, mid = gold-orange, tip w/ open above = deep red — burning pools read as licking tongues; flying embers' world-coord hash is bounds-guarded). Round 10 (user: "dots not streams"): **velocity-aligned motion smear in Particles.Draw** — grains >60 px/s draw as a rotated streak (~1 frame of travel, cap 6×size); overlapping smears fuse hose grains into continuous rope strands (a stationary square per grain reads as sprayed dots at ANY emission density); hose cones tightened (flame 0.24→0.17, acid 0.22→0.16 rad) + speed band 0.75-1.25→0.85-1.15 so smears bunch; flying-cell streak cap 4→6 cells. Rain/sparks inherit the smear (they're fast) — bonus streaking. Round 11 (user: "spraying stretched pixels super fast, want slow long streams"): hose jetSpeed reach*2.6→1.9 with grain life 0.4→0.6 max (same tongue length, half the flow speed) + Game1 payload launch reach*2.1→1.7 BOTH hoses (visible stream and cell payload must share an arc); smear ~2 frames (0.022→0.035) cap 6→12×size. Screenshot: stream = bundle of long parallel flame strands ✓. Round 12: **constant hose flow** — jetSpeed pinned `const 80f` (the tap-fire start speed; the hold-ramp had scaled it 80→250 with reach), hold now lengthens tongue via `lifeScale = reach/42` on grain Life; Game1 payload launch pinned `72f + rand` BOTH hoses (arc-match) → effective hose range is now the tap-fire range at every hold length (deliberate, per user). **MERGED cell-effects→noita-sim 2026-07-15** (~08:30): merge-race pattern worked (merged noita-sim INTO worktree first, resolved 3 conflicts — Program.cs probe arms both-kept, RunSave v24 over their v23 HasGrapple note kept, Game1 stepped-zoom + their titan fight-zoom COMBINED: fractional fight-zoom auto-falls-back to direct render path — suite PASS on merged tree, then merged back clean; auto-committer completed the conflict-resolution commit itself). Worktree still exists post-merge; both branches at same tip. Round 13 (user: "lags dropping from mothership + on landing"): the DESCENT runs the direct render path (fractional zoom lerp 0.72↔4, pixel-grid off) and the stride ladder still gave stride 1 down to zoom 1.8 → >1M cell candidates/frame mid-drop; FIX = direct-path stride kicks in below cellPx 1.6 (`_pixelK>0 || cellPx>=1.6 ? 1 : ceil(2.2/cellPx)` cap 10) + **AddLights stride floored at 2 everywhere** (seeds combine by MAX into 4px light cells — every-2nd seeding identical, halves the duplicate scan) + **interior LOD cut loosened stride>1→>2** in Draw/AddLights (stride 2 now occurs CLOSE-UP via fight-zoom/light-floor — the old cut would have vanished deep cells/lights in titan fights). Orbit over ember lava sea: 60fps Debug (drw 6.5ms). Ember ground: ~60 typical, dips to 40 in heavy fire scenes (Debug). Merged to noita-sim (2663974). Round 14: constant-flow REVERTED (user: wanted slow flow NOT short range) → jetSpeed reach*1.4 + payload reach*1.7 restored, lives 0.5-0.85s (travel≈reach at any hold), smear 3 frames/16×; the /btw fork verified-no-op (parent had already applied). Round 15 tune: StreamHoldMax 0.35→0.245s, reach 42-132→59-185px, ALL four spread cones ×0.4 (payload 0.072/0.064, glow 0.068/0.064 rad). Round 16 (user: "waves of 5 + stray yellow strands out-ranging the stream"): **de-pulse** = per-grain emission-time head start (`lead = rng*0.06s`, Position += vel*lead, Life -= lead) in BOTH hoses so puffs interleave into continuous flow; the overshooters were (a) hose-shed CINDERS at reach*0.9 with ember gravity 0.55 flying flatter+further than the 2.25-gravity tongue → reach*0.4, and (b) buoyant lick grains → seeded ≤reach*0.4, life ≤0.35, drag 3.2 (die within the tongue). Round 17: glow jetSpeed 1.4→**1.7 = EXACTLY the payload multiplier** (any gap = landing fire beyond/short of the visible tip — "the mismatch"; keep these two locked together) + **every hose grain stamps its material on landing** (flame grains + licks LandMat=Fire, acid droplets LandMat=Acid — the whole visible spray burns/corrodes, not just the 3 payload cells; acid self-depletion + fire budget bound it, but WATCH acid strength in playtest: ~8× more corrosive mass per puff). Smokeprobe airburst check made delta-based (ground-burst plume drifts into the count zone after 8 sim-s). All merged to noita-sim. Round 18: **rain decoupled from hose smear** — new `Particle.SmearMax` (world px, 0 = shared 16×size default) caps the motion smear per grain; EmitRain pins 3.3f (the original round-10 streak length, before the two hose-driven smear bumps). Future hose smear tuning must NOT touch rain — it has its own cap now. Round 19 (user: yellow embers back + want land-sparks): lick grains AND hose cinder shed REMOVED outright (both rode non-stream arcs — rising -0.5 / ember 0.55 gravity vs the tongue's 2.25 — and kept resurfacing as stray yellow strands; their burn-where-it-lands job is covered by the per-grain Fire stamp); new `Particle.LandSparks` one-shot flag → resting grain throws 3 tiny sparks in its OWN stepped-fade colour (kicked up along local normal + tangent; spawned sparks carry no flags so no cascade) — set on all flamethrower main grains. Round 20: rain SmearMax 3.3→6px + all rain grain colours ×0.72 (translucency = the "blur"; premult Color* scales alpha), fade ×0.4→0.3. Round 21 (yellow streaks ROOT CAUSE, screenshot-proven): aiming skyward, glow out-raced payload because flying cells obey **FlyMaxOutward=170** and particles didn't → hose emitters now take `up` and mirror the exact cap on grain velocity (KEEP glow+payload caps identical); airborne fire cells were the pale-yellow flicker (garbage tier probe at world coords) → ColorFor `airborne` param pins flying fire to mid tier; flow slowed reach*1.7→1.35 BOTH (user: still too fast), lives→0.95/0.92 max. Round 22: **flamethrower LaunchAtWorld payload REMOVED entirely** (user: still off-arc — flying cells can never share the particle arc exactly: FlyMaxOutward, separate jitter) — the grains ARE the flame now (lives 0.8-1.35s, every grain stamps Fire on landing, cone ignition burns creatures); acid spewer KEEPS its payload (corrosion mechanic). Compaction voided-pile test observed flaky (fails 1-in-2 runs, passes clean rerun — rng-sensitive settle, predates this change). Round 23: **EmitJetCore** — flamethrower + acid spewer are ONE shared stream core (tones/hotTones/fade/landMat/lights/drag are the only per-weapon params); acid payload ALSO removed (droplet Acid stamps = the corrosion mechanic now); cone 0.068/0.064→0.041 (-40%); hoses carry PRIVATE SmearMax 8.8 (= frozen old shared look) — **contract: hose tuning goes in EmitJetCore only and touches nothing else; shared Draw smear/fade changes no longer affect hoses or rain**. Round 24: falloff/landing spread -30% = speed band 0.85-1.15→0.895-1.105 (range∝v², so the SPEED band is the landing scatter, not the cone). Round 25 (stream detached from gun at full ramp): de-pulse position advance scaled with vel (≈15px at full hold) → capped 6px (temporal stagger keeps full range), seed jitter 5→1.5px, muzzle offset 8→4px both hoses. Round 26 (still waves, esp. moving+firing): **hoses fire EVERY FRAME** (ShootCooldown 0.05→0f) with 8 grains/frame (~same rate) — emission continuous by construction, de-pulse lead machinery deleted, grains born AT the muzzle; COMPENSATED: damage constants ÷3 (42/62/50/70→14/21/17/23, they're ×_frameDt per CALL so call-rate ×3 = dps ×3) and StreamHoldMax 0.245→0.735 (StreamReach adds _frameDt per call — the old ramp was secretly 3× the constant in real seconds; 0.735 real = same feel); hero flicker gated 1-in-3 frames (ray-cast budget), soot 6→2/frame, vapour 3→1/frame; strands 2× via new Particle.SmearScale (hose SmearMax 17.6). TRAP: any future per-call hose constant must account for per-frame calling. Round 27 ("shooting spaghetti while flying"): hose grains **inherit shooter momentum** (`vel += shooterVel` AFTER the outward cap on the launch component; soot/vapour wisps too; Game1 passes Player.Velocity) — world-relative launch painted the muzzle's flight path in fire; standing-still look unchanged by definition. Slow flow makes player speed a big fraction of stream speed, which is why this only surfaced after the slowdown rounds. Round 28: muzzle = DRAWN gun tip (grip 3.2 + texWidth-1.5 × 0.39 held scale, per-weapon via _weaponTex; the orb's aim*9f was the hint); births back-filled along shooterVel×rng×0.016 (moving-muzzle stair rows); grains -20% (skip 1-in-5); LandMat stamp chance 100%→50% both hoses. Round 29 ("fluid like water"): **hose streams render as metaball bodies** — grains carry `Particle.Fluid` (Material tag) + `Particles.FluidMode`; DrawFluid draws CAPSULES (len ≥5px / ~2 frames of travel along vel, width 4 — round blobs made a meatball chain, capsules bridge the inter-frame batch gap) into coverage RTs under LiquidFillBlend; ACID grains join _liquidRt (stream fuses with pools); FLAME gets own Game1._flameRt (fire must never metaball-fuse with water/acid), composited via the same CompositeLiquids shader right after pools; strand-quad fallback when shader off/zoomed out (FluidMode false). **Outward 170 cap DELETED from EmitJetCore** (bent aim + split arc families when firing up; payload gone, grains ≤1.35s). DM_LIQRT=0 kills pools+stream fluid together. Round 30: **EmitJetCore DELETED — hoses split into fully independent EmitFlameJet/EmitAcidJet per user** (tune individually going forward; the round-23 coupling contract is REVOKED); flame soot flecks + acid vapour wisps removed (fast enough to smear = read as leftover stream strands around the fluid body); remaining flame extras: hero flicker (invisible light carrier). Each emitter owns its full constant set inline. Round 31: **fire-cell BURN FUSE** — fire never uses _srcTile, so it carries a fuse there (TickFire: fuse>0 → no gutter roll, decrement 1-in-3/tick; ~3×fuse ticks guaranteed burn; rides the save free); StampAtWorld gains fireFuse param, Particle.LandFuse carries it; flamethrower grains stamp fuse 50-80 (~2.5-4s standing burn on ANY surface — spread still flammability+budget gated, ember-spit still fuelled-only). ALL fire-creation paths must zero _srcTile (IgniteCell did, gas-ignition now does). Smokeprobe cinder sampling 30→8 ticks (was missing 0.8s fire peaks). Round 32 (flame pooled like liquid on landing): (1) flame grains (Fluid==Fire) DIE 0.1s on rest — the resting-grain metaball puddle was the pooling; the fuse-burning CELL fire is the standing flame; (2) CompositeLiquids gained a param overload — flame RT composites opacity 1.0 + rim 1.85/+0.15 (hot sheath) vs pools' 0.80/1.45/0.08 wet lip; (3) flame land-sparks are BUOYANT licks (gravity -0.45, tangent 20, life ≤0.38) vs liquids' falling spatter. All flamethrower-only; acid untouched. Round 33: fuse 120-180 (6-9s ground burn), char rate 28→90 (~3× slower — burning structures burn DOWN not vaporise; ignition reliability = fuse dwell time). Round 34 fire-jet look (items 1-3 of 4): DrawFluid fire branch — blob width lerp 2.5→7px by age (taper/bloom), colour × lerp(1→0.5, age²) (faint tip blobs = coverage tatters at the threshold = ragged boiling end); Update — Fluid==Fire grains get bounded lateral turbulence (perp × sin(Life×55) × 90 × age × dt; Life-clock phases scatter free). Item 4 LANDED (round 35): 12Hz body flicker in DrawFluid fire branch — `c *= 0.85 + 0.25·sin(Time·75 + (Life+Time)·40)`; TRAP: the per-grain phase key MUST be `Life + Time` (constant per grain = birth time + initial life) — keying off Life alone strobes 60Hz white-noise as life ticks. Round 36 (user: 3D-game flamethrower, stream disappears into plume; ref = Blender FT tutorial vid): **EmitFlameJet rebuilt as CARRIER + PLUME** — 3 invisible carriers/frame (Size 0; unchanged arc physics; carry LandMat+fuse+LandSparks; own fire tones ONLY so the spark splash has ink — Transparent carriers made invisible sparks) + 6 plume puffs/frame (THE visible fire: launch with stream, drag 1.5 stalls them late-life, gravity -0.3 buoyant lift off the arc, FadeColor smoke-grey 75/60/55 so flame gutters into smoke, Fluid=Fire → metaball + turbulence + flicker; blob width now 3→11px by age = billow); wider plume cone (coneArc×2.2). Acid keeps the round-30 solid-rope form. Round 37: plume GravityScale -0.3→HoseArcGravity (young puffs DROOP WITH the stream) + **age-growing buoyancy in Update** (`vel -= GravityAt × 200 × 4.5 × age × dt`, Fluid==Fire only) → net 2.25g→-2.25g over life: fire tracks the arc then tears upward, divergence peaks at stream end; FlameTones deepened (255,235,150 / 255,180,50 / 255,120,25 / 230,75,20 — old set read gas-torch pale); +spark streaks (1-in-2/frame, 1.1-1.4× stream speed, SAME arc gravity, smeared, quick); +smoke billows — **ALL OF ROUND 37 REVERTED per user immediately after** (flamethrower is back at the round-36 carrier+plume state: pale FlameTones, plume gravity -0.3 buoyant, no divergence lift / sparks / smoke billows; restored via `git checkout <palette-commit>^ -- Particles.cs`). Don't re-apply round-37 ideas without the user asking. Round 38 (user then asked for two of them differently): plume FadeColor smoke-grey 75/60/55 → deep flame red 205/75/15 (**no black/grey anywhere in the jet**), DrawFluid tip dim floor 0.5→0.72 (heavy dim on red = brown-black; tips tatter via coverage instead); +spark pixels — Round 39: sparks now SHED BY THE PLUME GRAINS THEMSELVES (Update Fluid==Fire clause, 1-in-60 per grain per tick ≈ 5-6/frame at full stream; Position = grain's, Velocity = grain×0.6 + Jitter(45)) so they're born on the actual burning body — the earlier aim-ray seeding put them off the drooped/moving flame; smoke billows likewise shed sparks (Fluid=Fire) — acceptable, embers off smoke. Round 40: sparks get `Particle.JetSpark` — same turbulence wander as the jet body (shared clause `Fluid==Fire || JetSpark`; shed still gated `!JetSpark` = no recursion), SmearMax 1.5px (was ~8px default streaks), GravityScale = symmetric ±1.6 random (half curve up as hard as half sag — dancing flecks, not falling grit). Round 41: shed 1-in-60→1-in-20 (3× sparks, ~17/frame full stream); JetSpark added to the die-on-rest clause (0.1s) — lit sparks were parking 1.4s as glowing embers under the cinder rule and READ as igniting surfaces (they never stamped fire). Round 42: sparks CollideTiles=false — even the bounce/ricochet read as ground interaction; sparks are pure light now, only the carriers touch the world. Round 43 (STILL ground sparks — the real sources): (a) carrier LandSparks touchdown splash REMOVED (3-fleck burst at every landing = sparks born at ground contact); (b) spark shed speed-gated sp>3600 (grounded/stalled plume grains were shedding onto the terrain). Flamethrower ground contact = fire stamp ONLY now. Acid keeps its LandSparks liquid spatter (separate weapon, not complained about). Round 44 (jet wasn't igniting): carriers bounce at ~250px/s and often EXPIRED mid-skitter without ever resting → LandMat never stamped; FIX = fuse-carrying grains (LandFuse>0 = hose carriers only) stamp on FIRST CONTACT + velocity zeroed so the rest branch fires the contact spark burst same frame (LandSparks restored on carriers — it's the STREAM's touch effect, distinct from flying sparks which stay world-inert); char 90→45 (middle: touched surfaces catch within a beat, structures still burn down slowly). Rest-based handoff unchanged for cinders/debris. Round 45 (sparks at feet firing straight UP): carriers are BALLISTIC and outlived the visible plume — up-shots arced back onto the shooter (~1.1s return) stamping fire+bursts where no flame showed; carrier Life 0.8-1.35→0.65-0.85s = full horizontal range (~0.75s) but dies at the apex, return arc impossible. Round 46: **CARRIERS DELETED** (user: 'why have invisible carriers at all' — every phantom-fire bug traced to their ballistic arc mismatching the buoyant visible flame): plume puffs ARE the weapon — 50% carry LandMat+fuse+LandSparks and stamp on FIRST CONTACT (the fuse>0 contact trigger); buoyant puffs can't arc back → up-shots structurally can't rain fire on the shooter. Ignition range = where visible flame reaches (~jetSpeed/drag ≈ stream length) — expectation-aligned by definition. The carrier concept is DEAD, don't reintroduce it. Round 47 (licking burn-site flames): **fuse top-up** — StampAtWorld re-stamp onto an already-burning fire cell adds fireFuse/2 capped 80 (=4s fuel-less; initial stamp fuse 30-42 ≈ 1.5-2s) → burn duration IS jet dwell time; **Cells.PendingFlames** (PendingBubbles pattern, cap 200) — burning cells (fused OR fuelled) queue 1-in-12/tick → Game1 drains → Particles.EmitLickingFlame (rising tongue, Fluid=Fire joins metaball body, no world interaction, below spark-shed speed gate) — standing fires read as actual writhing FLAME and the visuals spread wherever fuel spreads the real fire. Round 48: licks are NOITA PIXEL FIRE not fluid (Fluid tag removed — ground flame = crisp 0.5-0.8px grains flickering up the stepped ramp; only the JET is a metaball body; licks carry JetSpark for the turbulence wander + SmearMax 1.2, no collision); jet spark shed 1-in-20→1-in-7 (3× again, ~50/frame full stream). Round 49: shed 1-in-7→2-in-7 (2×, ~100/frame full stream); spark velocity 0.6→0.78 stream inheritance + Jitter 45→31 (30% more in-line, less radial flying). Round 50: shed →4-in-7 (2× again, ~200/frame full — spark counts getting perf-relevant, watch pool size), Jitter 31→26 (envelope -15%). Round 51: plume TAIL-END = black smoke (DrawFluid fire branch: age>0.7 hue-shifts to 48/43/44 in TWO hard steps, alpha/coverage preserved so the wisp keeps shape; body stays pure fire — this coexists with round-38's "no black" because it's the die-off only, per user). Round 52 flame-sites tried and REVERTED next round ("did almost nothing" — ~8 half-px flecks alive per site is invisible). Round 53, the working answer: **density of WORLD fire, not more particles** — `Cells.StampFireBlob` (contact stamps 5 fused fire cells in a 2.5px disc; occupied fire = fuse top-up) so the ground itself burns Noita-style (cells dance/tier-shade); PendingFlames queue rate 12→5 + lick life 0.4-0.75/speed 18-44 (licks ride the denser cell population). Lesson: burn visibility scales with BURNING CELLS; particles alone can't carry it at 0.55px cap. Round 54 (flame shape): EmitLickingFlame tapers like a real flame — base offset s0 ±2px, side velocity −s0×5 (drag bleeds it) so paths CONVERGE rising, outer licks' life ×(1−|s0|×0.12) (edges exist only low → triangle silhouette), 1-in-4 TALL tongue (up 34-64 vs 18-36, life 0.55 base) = the dancing tip. Round 55 (base too dim, fades too low): licks + jet sparks now set **MaxLife = initial Life** — with a fixed MaxLife most were BORN mid-ramp (t0 = Life/MaxLife < 1 = pre-cooled); now every one starts white-hot at the base and cools across its own full path. TRAP: any emitter whose Life is randomized below its MaxLife spawns pre-faded — check t0 when adding emitters. Round 56 (strongest part must STICK to the surface, die top→base→smolder): **fuse-driven flame pyramid** — PendingFlames entries now carry the cell's remaining fuse (fuelled floor 55); EmitLickingFlame(pos, up, fuse): strength=fuse/80 → 3 layers: 55% BASE CHURN (near-stationary white-hot grains boiling AT the surface, gravity 0, rise 3-10), 30% rising body (rise ×strength), 15% tall tongues ONLY while strength>0.5; strength<0.25 = SMOLDER (dim ember reds 200/70/20 & 150/45/18, hugging surface). Round 56 pyramid layers REVERTED (round 57, "did not work as intended"): back to the round-55 taper shape; new rule = **vigor = 0.45 + 0.55×(fuse/80)** multiplying BOTH lick lifetime and velocity — as the ORIGIN's fuel drains, everything risen above it expires faster and hugs lower, while the origin's fuse-backed burning cell outlasts it all (the user's model: origin holds the most fuel, dies last). Round 58: vigor scaling UNDONE per user; the settled design = **height-ambition continuum**: per spark h=rng — rise lerp(3,52,h²), lifetime INVERSE lerp(0.85,0.25,h)×(0.8-1.2 jitter), buoyancy -0.35×h, convergence (1.5+3.5h) — long-glowing base-huggers at the origin, brief tall tongues, full spread between; fuse param kept but UNUSED. Round 59 = the REAL fix (rounds 57-58 were symptom-chasing, both reverted): TickFire's upward flick moved FIRE CELLS off the surface with the fuse riding along — the origin went dark while the long burn finished mid-air (licks emitted at cell positions followed it up). Fix: **flick gated on _srcTile[i]==0** — fused (flamethrower) fire is ANCHORED to its ignition surface for the whole burn; unfused transient fire keeps dancing. Licks back to round-55 taper shape. Origin now structurally dies LAST. Round 60 (anchoring crippled fuel spread — the flick IS fire's transport): **three-state fused fire** — `anchored = fused && !fuelled && grounded` (grounded = any solid-tile neighbour, flagged free in the probe pass): (1) barren surface → anchored origin burn; (2) FUELLED → released to travel/spread, fuse still blocks gutter (stronger front than plain fire); (3) mid-air unfuelled → fuse drains -3/tick (~0.1s) + keeps dancing — no hovering anchors when fuel depletes in the air. Known edge: fire atop loose CELL piles (sand) counts ungrounded → fast drain (acceptable; extend grounded to matter cells if it reads wrong). Round 61: fuelled fire = INTENSE but SLOW-consuming — PendingFlames roll fuelled 1-in-2 vs bare 1-in-5 (2.5× licks over fuel), fuel-cell ignition 1-in-3→1-in-6 (pools last ~2× longer), tile char 45→60 (structures burn longer; catch reliability still carried by fuse dwell). Round 62 (fire couldn't spread DOWN): flame movement was up-only, so fire FLED fuel beside/below before slow ignite/char rolls landed; fix = fuelled fire's moves are 1-in-3 lateral-or-downward (dirRoll: down/left/right via InnerCell/±cx), rest keep the rising dance — fire now burns down trunks and descends oil films while unfuelled flame still only rises. Round 63 (trees STILL only burned up — crawl can't enter solid trunk; the real asymmetry is RESIDENCE TIME: rising fire piles against ceilings but evacuates floors): **cling** — flame whose floor tile is flammable (Probe below:true sets burningFloor) gets mobility ÷3, + **floor char 30 vs 60** — trees now burn down the trunk; up still faster (natural). Round 64 (CONTRACT: small tree fully aflame before first-lit part burns out): charring IS consumption so it can't be the spread mechanism — **BUDDING** added (fuelled flame births fire into a random free cardinal neighbour 1-in-25, SpendFire-gated) = direction-neutral engulfment decoupled from burn-through; fuelled gutter 56→130 (~2.2s front persistence); consumption slowed further: fuel-cell ignite 6→10, char 60/30→110/55; fuelled licks queue EVERY sample (was 1-in-2). Suite PASS ('fire gutters out' still green — budget caps the blaze). Round 65: **CONSUMPTION BUDGET split** — char rolls scale with flame population and budding GROWS the population, so no char constant could stop an engulfed tree eating itself (and chars fought buds over one budget); new `_charBudget` (cap 6, regen 2.5/s planet-wide) — BOTH char sites (TickFire probe + lava-side TryIgniteTile) spend it; rolls loosened back to 40/20 (budget is the limiter; lone burns stay responsive). Consumption is population-independent BY CONSTRUCTION: engulfment (~2s, SpendFire-budgeted buds) always beats burn-through; forest fires smoulder at the same global 2.5 tiles/s. Round 66 (too slow both ways): CharBudget 2.5→6/s (cap 12), budding 25→12 (engulfs ~1s), fuel-cell ignite 10→6. A ~15-tile tree: engulfed ~1s, consumed over ~3-4s — aflame-before-burnout contract still holds with margin. Round 67 (missed connected fuel + still slow): (A) **fuel-seeking buds** — Probe records a cardinal fuelMask; buds travel PERPENDICULAR to the fuel (fuel above/below → bud sideways, beside → vertically), wicking along surfaces instead of blind-random; rate 12→7. (B) **8-direction probes** — 4 diagonal Probe calls (InnerCell/OuterCell of cx±1, dirBit -1 = no mask steer): canopies connect at CORNERS which cardinal probes could never reach — the root cause of surviving fragments. (C) char budget 12/s cap 24 (2×). TickFire probes now 8-9/cell/tick — fire pop is bounded, fine. Round 68 — THE REWRITE (user: engulf-then-burn-out arc; emergent tuning had failed 6 rounds straight): **Cells.BurningTiles registry** (tile→clock; cap 400) + TickBurningTiles sweep: ignite → after 0.25s ignite all 8 neighbours (deterministic fuel-graph wavefront) → blaze 4-7s hash-stable (licks 1-in-5/tick + hazard fire cell 1-in-30 in adjacent sky) → char to Sky+fire+smoke+ShedBurningLeaves. RETIRED: budding, cling, fuel-seeking mask, diagonal probes, char budget (probes back to 4 cardinals; TickFire flammable branch + lava TryIgniteTile now just IgniteTile 1-in-20/24 SpendFire-gated; jet StampFireBlob ignites 3×3 tiles directly). **Rigid persistence**: BodyCell.Burn (clock+1; rides splits since it's IN the struct), SpawnBody steals via Cells.StealBurning, Stamp re-ignites at the LANDED tile (clock resumes), tumbling bodies QueueFlame — fire survives detach/fall/land. Registry NOT serialized (flying-cells policy). Suite+smokeprobe PASS. Tuning knobs: BurnSpreadDelay 0.25 / BurnDurMin 4 +3 var / MaxBurningTiles 600. Round 69 (stalls/uneven/eternal impotent flames): spread was a ONE-SHOT clock crossing — any ignition blocked at that tick (chiefly the 400-cap during a healthy blaze) was never retried → unlit adjacent fuel + fuelled cells lingering impotent; also resumed rigid clocks skipped the window entirely. FIX = spread retried all blaze long (`nc >= delay && rng18==0`, idempotent dict hits) + cap 600 — the cap is SOFT now, blocked ignitions heal as burn-outs free slots. RULE: never one-shot an event whose success depends on transient global state. Round 70 (big fires capped out, front stalled): **pressure aging** — clocks run ×(1 + (count − cap/2)/(cap/4)), max ×3, when the registry fills; the TAIL burns out faster to free slots so the FRONT keeps full pace (travelling fire band); cap 600→900. Front spread also quickens slightly under pressure (delay crossed sooner) — reads as a big fire raging. Round 71: **burn-out is structural** — Cells.PendingCharred (tx,ty,kind) queued on every char-out; Game1 drains → Physics.MarkDirty (settle detaches undercut regions as rigid bodies — fire severs like a pickaxe) + TreeTrunk → ToppleTree (rigid log fell); the felled log's burning tiles ride BodyCell.Burn and keep burning where they land. Burn-through a tree's base and the whole thing topples aflame. Round 72: falling-body flame was thinning at detach (double-gated licks ~5/s vs standing tile 12/s, and no hazard fire-cell layer on moving bodies) — fixed with tile-parity licks (Next(5) per burning cell per tick) + 1-in-30 ember shed via LaunchAtWorld(Fire) inheriting 0.8× body velocity, so tumbling logs trail embers and can spot-ignite mid-arc. Round 5 (user: "stream out of the gun should look the same as landed"): ColorFor gains `skin` param — EXPOSED + AIRBORNE liquids drop the ×0.78/0.82 translucency and brighten (+24/+20 tint) so flight ink == landed-skin ink (pool INTERIORS keep translucency = depth); sub-grains get ±0.21px hashed scatter (kills the neat-lattice "landed on a grid" read) + 1-in-8 white glints on liquid skin (continues the hose's bright leading droplets onto the ground).

Deliberately NOT done (discussed with user): rigid-body chunks (would fight the dust/compaction economy, see [compaction-and-tiles](compaction-and-tiles.md)), blood/stains on bodies — user later approved wet/oily STATUSES (mechanical + subtle tint), which are in.

2026-07-15 titan-siege / climbing / clouds-v2 / branches wave (RunSave v23):
- **Clouds v2** (Weather.cs + Game1.DrawWeather): `Cloud.Alt` is now an ABSOLUTE radius from planet centre, fixed at spawn (BandTopRadius + 110 + rand) — no per-frame terrain-tracking lerp, no sine bob (both caused "bouncing"). Draw = many small puffs (arcLen/11) packed along the ARC at that one radius (flat bottom, connected; deterministic per-puff wobble only). Drifting into terrain that reaches the ride band (mountain/skyscraper) sets `Dissipating` → rain stops, Life clamps to 5.5s and the existing Grow fade shreds it slowly.
- **Tree branches**: TreeEcology.StampBranches (called from BuildTrunk when canopy stamps, so regrowth earns them back at full height) — trees ≥7 tall get alternating side boughs every 2-3 rings (1-2 TreeTrunk tiles + 4-tile leaf tuft), deterministic `new Random(GroundR*92821 ^ (int)(Angle*1e5))` so regrow matches. SimTest "a tall tree grows side branches".
- **Titan siege** (Titan.TickSiege, every walker): KICK low architecture (shin-height fan scan) by driving a planted leg's step onto the wall base — the step machinery IS the animation, landing runs KickImpact (Mine power 60 = one-blow alloy breach, quake, shockwave, pulverize). Arm kinds (`HasArms`: Godzilla/Mecha/Kong/Leatherback/Slattern) also HAND-SMASH high storeys (+10..100px fan) — Kong's SmashTimer machine moved to TickSiege for all of them, power 12→48 r 2→3; TitanRenderer.SmashFist shared helper animates the committed arm in all four kinds' draw fns. Plow aggro 26→34, wreck cadence 0.12→0.09 (power stays 22 — 2 bites in 0.15s must NOT breach or the SimTest first-instant pin fails; 255-damage math: power*32/6 per bite), StompTile floors arch. tiles at power ≥20.
- **RigidBodies.Pulverize(pos,r)**: cells inside r spill to dust (SpawnDustFraction 0.6), remainder re-surfaces/splits/dies like Erode — consumed from `Titan.PendingPulverize` by Game1 (kick+smash grind toppled debris to powder).
- **Roars**: Sfx "roar" (detuned-saw beat + sub + skreonk overtone), "screech" (flyers/Raiju/Otachi — `RoarVoice`), "thud" (kick/fist bass). `Titan.PendingRoar` (??= so no stacking) set on kick/smash/shake + generically when ANY special's windup arms (SpecialState crosses 0, one hook in Update).
- **Weakpoints**: `Titan.WeakpointsWorld()` (nape/dorsal/tail-root off Radius+Facing; worm = TailNodes[1]; empty unhatched/submerged), WeakpointRadius 18. Combat.ApplyDirectDamage now takes hitPos (PrevPosition + seg*t) → ×3 damage + WeakpointFlash inside one. TitanRenderer draws pulsing orange pips (white on flash). SimTest "weakpoint hit pays triple".
- **Riding**: Game1.TickTitanRiding — an AIRBORNE dwarf touching the hull latches to a bearing on the BodyRadius circle (grounded contact never latches — titan walking into you is safe); A/D walk around the hull, W hops off, jetting pulls free; position/velocity follow the body.
- **Shake-off (EVERY kind)**: Game1 accrues `RiderTime += 2*dt` while riding/grapple-latched, titan decays 1*dt; >2.2s → ShakeTimer 1.2s (plants, vTangent = sin(ShakeTimer*22)*150 thrash), `PendingShakeOff` fires mid-thrash → Game1 flings (up*240 + right*±300, 6 dmg, releases grapple), cooldown 6s. SimTest "clinging rider gets shaken off".
- **Grapple + rope**: Player.HasGrapple (RunSave v23, written right after HasLightningGun). "grapple" item (Tools category, icon, recipe iron5/hide2/wood2, godmode grants): FireGrapple ray-marches 400px → latches first BlocksPlayer tile or the titan hide (`_grapLocal` body offset). TickGrapple = hard length constraint (kill outward radial vel, keep swing); held-LMB reels 130px/s, S pays out, W cuts, auto-release on mined anchor/450px/boarding. Drawn as a rotated 1.2px hemp rect + claw dot. "rope" item (stack 8, recipe wood2/hide1): PlaceRope unrolls ≤10 `TileKind.Rope` (58) tiles down from any non-sky anchor — Rope is anchored+passable+climbable (`Tiles.IsClimbable` now Ladder|Rope; Player.Update onLadder uses IsClimbable), authored swaying-strand render (UsesAuthoredArt).
- **Titan-fight camera**: `_fightZoomBlend` (Game1.Space.cs field) eases 0↔1 while the hatched titan is <760px; zoom = lerp(_playZoom, max(2.2, _playZoom*0.68), blend) — replaces the bare `_camera.Zoom=_playZoom` in the play update.
2026-07-15 jump/jet controls REVERTED to Space-only (user request; supersedes the 07-14 "W-jumps/Space-jets split"):
- **Space = jump AND jetpack throttle, Noita-style**: tap jumps; a grounded hold lights the pack after `JetHoldDelay` 0.18s; an AIRBORNE press lights it instantly (`_jetPressAirborne`, set on the press edge when `!Grounded && coyote expired` — coyote presses count as jumps). W/Up no longer jump — they only climb ladders, swim-stroke via verticalAxis, steer fly mode, and menu-navigate; rocket-ascent thrust still takes W/Up/Space.
- **Player.Update lost the `jetHeld` param** (5-arg again). Callers: Game1 play update, DM_JETTEST (now just `jumpHeld=true`), 6 SimTest sites (swim/platform). Titan-ride hop-off and grapple-cut moved W→Space (Game1 TickTitanRiding/TickGrapple).
- Pre-existing failures at this point (NOT from this change, verified in a worktree at the pre-edit commit): `city: tower facades draw dead straight (wobble 4.20px)` and `city: one capital plus smaller towns (groups 1/7/7/13)` — parallel session's territory, see [city-facades](city-facades.md).

- **TRAP confirmed**: `hazard: acid dissolves a soft tile` is FLAKY by construction — Cells `_rng = new Random()` (time-seeded) + acid self-neutralising 2-in-3 per bite can spend the whole pour before the pinned floor tile breaks. Failed once, passed on rerun with identical code. Also: the auto-committer commits per-file WHILE working — `git stash push` mid-wave found almost nothing to stash; don't use stash-vs-HEAD as a control experiment here.

2026-07-15 LIQUID COHESION pass (user: "water looks like stacked pixels, Noita's looks fluid and all together") — diagnosis was RENDERING, not sim: translucent 0.78-alpha quads with 1.5× seam padding double-blended at every overlap (grid of darker halos) + per-cell hash jitter/hash-phased shimmer made the lattice readable:
- **Liquid RT pass** (`DM_LIQRT=0` reverts): Water/Acid/Oil grid cells skip the main cell batch (`Cells.Draw(..., skipLiquids:)`) and rasterize via `Cells.DrawLiquids` into `Game1._liquidRt` (sized to the active world target; filled BEFORE the world target engages so no mid-frame RT-switch content loss — order: fill liquid RT → SetRenderTarget(world) → DrawWorld → `Renderer.CompositeLiquids` → BeginEntities). Composites over the world in ONE alpha blend. stride==1 only; zoomed-out LODs keep liquids in the main draw. Flying liquid cells stay in the main pass (airborne streaks, not pool body).
- **Metaball composite shader** (`RuntimeEffect.BuildLiquidComposite`, LiquidGlsl; `WriteMgfx` now parameterized — TRAP: header effectKey must DIFFER per effect or the second reuses the first's compiled shaders, now `^ technique.GetHashCode()`): cells touching air draw `Renderer.LiquidBlob` (16px soft radial, white RGB, profile `clamp(1.3*(1-r*1.96))` ≈ 0.45 one cell out) 3 cells wide under `Renderer.LiquidFillBlend` (colour REPLACES, alpha ACCUMULATES = coverage field); interior cells stay flat 1.5-padded quads. Shader thresholds coverage at 0.40 (lone droplet keeps its texel; droplets 1 cell apart FUSE), brightens texels whose 4-neighbourhood dips below (Noita rim), outputs premultiplied at uniform opacity 0.80. Shader null → hard quads w/ per-material alpha in A + BlendState.Opaque fill + NonPremultiplied blit (still single-blend).
- **Flat body colour**: `Cells.LiquidBody(m,cx,cy)` — one colour per liquid + shimmer whose phase is smooth in ring angle (INTEGER multiple of ang so it's continuous across the wrap; flying cells' world-px coords bounds-guarded). ColorFor water/acid/oil now delegate (×0.78/0.82/0.94 for the fallback path). Hash jitter GONE for liquids.
- **Continuous waterline**: surface cells (open outer) collected during DrawLiquids, drawn after bodies as wide overlapping bands (chord×2.2) bobbing on `sin(t*speed − ang*waves)` with waves = n/24 INTEGER (wrap-continuous) — one undulating line, replaces per-cell bobbing squares (old per-cell block still lives in Draw for the fallback path).
- **Sim**: (a) PLUNGE — falling liquid meeting its OWN pool SwapCells downward (1/2 chance, ≤2/tick, different liquids never — buoyancy owns that) so waterfalls dive in instead of stopping dead; SwapCells clears kinetics so post-plunge impact splash is naturally 0. (b) `DispersionFor(m)` per-material viscosity: water Density (8), acid 5, oil 4, lava 3 (was flat 6) — water levels fast, lava creeps.
- Verified: full --simtest PASS (oil buoyancy/splash/aquatic all green), DM_AUTOSTART=debug DM_SWIM=1 DM_AUTOSHOT screenshots — body one flat translucent mass, rim traces choppy surface, rain splashes fuse into blobs. DM_SHADER=0 fallback runs clean. NOTE: DM_SWIM=1 on ocean world = dwarf eaten by sea monster before the shot; debug world's lake worked (he drowns after ~30s, shots land first).

2026-07-15 RAIN JOINS WATER BODIES (same session, user: "make rain pool into water and add to bodies of water"):
- **Absorption rule** (`Cells.JoinsWaterBody`, checked in TickLiquid before the evaporation roll): a RAIN-tagged cell touching untagged (permanent) water on any cardinal side — or buried under a FULL TILE (Density cells) of continuous water — sheds its tag (`_srcTile=0`) + WakeNeighbors so the untag spreads through the connected puddle by the same contact rule. Showers permanently raise lakes/seas; a basin filling ≥1 tile deep becomes a real pond from the bottom up. **Depth bar is deliberate**: first cut used "any sandwiched cell" (3 cells = 1.5px) — that would turn every dip permanent and kill the evaporation sink (the creeping flood the tag exists to prevent).
- **`DripWaterSrc = 254`**: ceiling-drip water re-tagged (was RainWaterSrc) — evaporates like rain but NEVER converts. Drips mass-duplicate from the pool above by design; an absorbing drip = unbounded cave flooding under every lake. Snow-melt water stays RainWaterSrc (bounded by the snow spawn cap, may join lakes).
- SimTest `TestRainPooling`: shower over an obsidian pool pocket → count rises permanently after 20s calm (3263→3364); identical DRY pocket at another angle → thin film dries to ~1 cell. TRAP: first control build was a floating sky-band obsidian "tray" (surfaceR+8, rim on a different-tile-count ring) — rain never stayed in it, 28/30 "survived" and looked like an evaporation bug; carve test fixtures as GROUND pockets by angle, not sky shelves.

2026-07-15 WATER-SURFACE INTERACTION (same session, user: "acid and fire effects interact with water again + rain goes through the water surface"):
- Root cause: PARTICLES (`CollideTiles`) only collided with solid TILES — hose flame/acid streams and rain streaks sailed straight through water CELLS into the pool body (glaring once the liquid RT made pools one cohesive mass). Payload flying CELLS always interacted (flying Fire→steam+bubbles on water, acid deposits on surface — intact, untouched).
- **Particles.Update water-crossing branch**: colliding particle ENTERING water (`Cells.WaterAtWorld(next) && !WaterAtWorld(pos)` — the entry gate exempts grains born underwater: mining chips, rising bubbles, else they'd die at spawn) stops at the surface: LandMat=Fire → stamps SMOKE (steam wisp) at the last position above the waterline; other LandMat (acid) stamps there = settles ON the surface; fast grains (>40px/s) throw 2 white spray sparks; then Life≤0.1.
- **Cell-side splash on pools**: splash threshold ×0.35 when the landing cell rests on liquid (rain contact speed ~224 cells/s vs SplashMinImpact 240 — never crowned on water before); + **plunge speed-gated** to `fallSpeed > TerminalCells*0.6` so only waterfalls dive — gentle drops (rain/drips) rest on the surface and splash instead of silently vanishing under it.
- **`DM_RAIN=1`** (Weather.ForceRain): clouds form in 1.5s + shower continuously — the rain-verification hook. Screenshot-verified: streaks end AT the waterline, spray crowns above it, clean body below. Full --simtest PASS.
- Tuning follow-up (same day, user): splash crowns ×0.7 height (both the particle spray 22-52→15-36 up-speed AND the cell crown's up-component when landing on liquid); raindrops softened (head white-lerp 0.35→0.18) + more ink (×0.72→×0.82); **debug-menu cloud spawns** — `Weather.SpawnDebugCloud(run, RainKind)` (fully-formed Grow=1 cloud over the player, RainTimer=16 rains immediately, `Cloud.KindOverride` beats the biome kind so an acid drizzle works on any world; airless still clears it) wired as two Game1 BuildDebugEntries ("Cloud — rain shower / acid drizzle (starts now)").
- Round 2 (user: "pooling barely noticeable + give rain depth"): **water deposition ×9** (Weather.Rain: 1-in-3-ticks trickle → 3 SpawnRainWater cells EVERY tick ≈ 180 cells/s/cloud; old rate was ~7s per single TILE of water at Density 8 — arithmetic said invisible; snow keeps the trickle, drifts have no evaporation counterweight). **Rain depth illusion** (EmitRain): per-streamlet `depth` roll drives speed ×0.75-1.15, ink ×0.55-1.0, Size ×0.6-1.0 (UNDER the 0.55px pixel cap — thin/faint = far; "bigger" is banned by the Noita grain rule so near = full-ink+fast+long-smear), GravityScale ×0.8-1.1, SmearMax 3.5-7. Screenshot-verified: mixed long-bright/short-faint volume + blue water banking on mountainside ledges mid-shower.

2026-07-15 rain/lava tuning round 3 (user: bubbles bigger, rain still through water, ground bounce too high):
- **Rain STILL leaked through water** after the entry gate: the gate needs `!WaterAtWorld(pos)` on the exact crossing frame, but the sea's TOP CELL LAYER CHURNS (waves, rain joining) — a drop threads a momentarily-open surface cell, next frame BOTH samples read water, the crossing never fires, and the drop streaks through the whole body for its remaining ~1.5s (screenshot-confirmed streaks deep in the ocean). Fix: `p.Backdrop` grains (rain — never born underwater) bypass the crossing test and die WHENEVER found in water; everything else keeps the born-underwater exemption (mining chips, bubbles).
- **Rain ground-splash bounce**: generic reflect restitution 0.4 → 0.33 for Backdrop grains only (snow doesn't collide, so this is rain-only); height ∝ v² → hop lands ~2/3 of old peak ("50% too high" per user).
- **Lava bubble domes doubled**: Cells.DrawHotLiquids dome width 5+hash*1.2 → 10+hash*2.4 cells (screenshot-verified fat blisters on a volcano crater pool).
- Verification recipe: `DM_AUTOSTART=ocean DM_RAIN=1 DM_AUTOSHOT=12` for rain-over-sea; **`DM_AUTOSTART=ember DM_VOLCANO=1`** spawns beside the first volcano vent = instant lava-pool view (crater full of lava, no hiking). NOTE: macOS has no `timeout` — background the run with `& pid=$!; sleep N; kill $pid`.
- Round 4 (user: bounce STILL too high + rain goes through LAVA/ACID pools): backdrop restitution 0.33→**0.15** (~1-2px hop, "dies in its splash"). Pool gate generalized to ALL liquids: new `Materials.IsLiquid` (Water/Lava/Acid/Oil — new liquids belong in this list) + `Cells.LiquidAtWorld`/`LiquidKindAtWorld`; Particles' surface branch keys off the KIND — fire-carrier grains quench to Smoke on water/acid, **IGNITE an oil film** (StampAtWorld Fire+fuse), and are swallowed silently by lava. DM_VOLCANO spawn is on the FLANK (+0.09rad) — crater-in-frame is spawn-RNG luck, the earlier crater shot was a fluke; lava/acid verified by shared-code-path + full simtest PASS, water screenshot-verified.
- Round 5 (user: splash sinks through the top layer + pooling droplets spawn at splash height): the round-4 "crown at `next`" overshot — `next` is a whole frame-step (~4px) past the boundary; now the death frame MARCHES the step 1px at a time and uses the first liquid sample as the contact (crown + drop death both). Weather cell-rain lift cut 4-14px→1-3px above FindSurfaceSpawn (which already carries ~6px body clearance) — the pooling cells were materialising mid-air. Crown up-speed also ×0.7 again (15-36→10.5-25) and the cell crown onLiquid factor 0.7→0.49; cell-rain deposition gated on Cloud.RainElapsed>1.4s so puddle/pool splashes can't fire before the first visible drops land (they spawn at cloud height and take ~1.5s down).

